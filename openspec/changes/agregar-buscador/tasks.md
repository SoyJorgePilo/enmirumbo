# Tareas: agregar-buscador

- [ ] 1. Extraer de `src/lib/slug.ts` el quitado de acentos a un helper compartido (NFD + borrado de marcas combinantes), sin cambiar el comportamiento de `slugify`. Verificable: `tests/slug.test.ts` sigue en verde sin tocar sus casos.
- [ ] 2. Crear `src/lib/busqueda.ts` con la normalización de texto (`normalizarTexto`: sin acentos, minúsculas, todo lo que no sea letra o dígito a espacio, espacios colapsados) y el troceo de la consulta en raíces (`terminosDeBusqueda`: recorte a 60 caracteres, máximo 4 términos, se descartan los de un solo carácter, raíz de 5 letras para los términos de 5 o más). Sin dependencias nuevas (`design.md` §2). Verificable: tests unitarios con acentos, "ñ", mayúsculas, signos, `%` y `_`, emojis y otro alfabeto, cadena de miles de caracteres, muchos términos, término de una letra y consulta de puros espacios (todas devuelven lista vacía o acotada, ninguna truena).
- [ ] 3. Agregar a `prisma/schema.prisma` las columnas `nombreNormalizado` y `queOfrecesNormalizado` (texto, no nulas, default `""` solo para poder migrar filas existentes) y generar la migración. Verificable: una base desde cero migra y hace `npm run db:seed` sin error; el cliente de Prisma expone los dos campos.
- [ ] 4. Escribir en `src/lib/busqueda.ts` la función única `datosDeBusqueda(nombre, queOfreces)` que devuelve el par de valores normalizados listos para guardar, y usarla en el alta del registro (`src/lib/registro/`). Verificable: un alta con "Plomería Güicho" queda con `nombreNormalizado` "plomeria guicho"; un alta sin "¿Qué ofreces?" queda con cadena vacía, no nula; un envío que manda esos campos extra no los altera.
- [ ] 5. Crear el script de relleno idempotente (`npm run db:backfill:busqueda`) que recalcula las dos columnas de todos los negocios guardados y avisa cuántos actualizó. Verificable: sobre una base con filas previas (columnas en `""`), la primera corrida las llena y la segunda no cambia nada ni toca otros campos.
- [ ] 6. Actualizar `prisma/seed-demo.ts`: escribir las columnas normalizadas con `datosDeBusqueda` y asignar giros del catálogo a un par de negocios publicados, incluido uno cuyo giro NO aparezca en su nombre ni en su "¿Qué ofreces?" (p. ej. la fonda con el giro "Comida corrida") y conservando al menos un negocio con acentos en sus palabras clave. Idempotente también en los giros (`design.md` §3). Verificable: correrlo dos veces no cambia el conteo de negocios ni duplica vínculos de giro; `npm run db:seed` sigue dejando la tabla de negocios vacía.
- [ ] 7. Escribir el test de consistencia de las columnas normalizadas: para cada negocio de la base de pruebas, el valor guardado es exactamente `datosDeBusqueda` de sus campos fuente. Verificable: la suite falla si se crea un negocio por un camino que no usa esa función (la red que protege al buscador de futuras escrituras, `design.md` §1).
- [ ] 8. Agregar `buscarNegociosPublicados(consulta)` a `src/lib/directorio.ts`, con `estado: publicado` por construcción como el resto del módulo, exigiendo todos los términos (cada uno contra nombre normalizado, "¿Qué ofreces?" normalizado o el `slug` de sus giros), orden `publicadoEn desc` + `nombre asc`, y la misma proyección de campos públicos que el listado (sin las columnas normalizadas). Verificable: tests contra la base de pruebas de "plomero" → el de "plomería" y el de "plomeria"; "futbol" → el club de "fútbol"; "pinatas" → el de "piñatas"; búsqueda por giro que no está en nombre ni palabras clave; dos términos exigidos ambos; un `en_revision` y un `rechazado` que coinciden y nunca vuelven; consulta sin términos → lista vacía sin tocar la base.
- [ ] 9. Crear el componente del buscador en `src/components/directorio/` (Server Component, sin `"use client"`): formulario GET hacia `/buscar` con `input` de tipo búsqueda, etiqueta visible asociada "Busca lo que necesitas", ejemplo dentro del campo "ej. plomero, tacos, futbol infantil", botón "Buscar", ambos con área táctil ≥44px, y valor inicial opcional para poder prellenarlo. Verificable: se renderiza aislado en test con y sin valor inicial; el HTML trae los tres literales y el `action` a `/buscar`.
- [ ] 10. Insertar el buscador en la home (`src/app/page.tsx`) arriba de la sección "Busca por categoría", sin encabezado propio. Verificable: el HTML de la home sigue con exactamente un `h1` y tres `h2`, el formulario aparece antes del encabezado "Busca por categoría", y `tests/layout.test.ts` sigue en verde.
- [ ] 11. Crear `src/app/buscar/page.tsx` (Server Component, lectura por request): lee el parámetro de consulta (primer valor si viene repetido), muestra el buscador prellenado, el `h1` `Resultados para "<consulta recortada>"` y las tarjetas de `TarjetaNegocio` con el mismo enlace de ficha y de WhatsApp que el listado. Verificable: `/buscar?q=plomero` lista los negocios sembrados que coinciden, en el orden esperado, y la consulta se muestra escapada como texto (`<b>plomero</b>` no genera marcado).
- [ ] 12. Agregar el estado sin resultados con sus literales: `No encontramos negocios para "<consulta>".`, "Prueba con otra palabra o elige una categoría:" y los 8 botones de categoría del catálogo enlazados a sus listados (los mismos de la home). Verificable: test de una búsqueda sin coincidencias que muestra los dos textos, los ocho enlaces y sigue respondiendo 200.
- [ ] 13. Agregar el estado de consulta vacía: `h1` "¿Qué estás buscando?", aviso "Escribe qué necesitas y te decimos quién lo hace en Tizayuca.", buscador vacío y las categorías; sin listar ningún negocio. Verificable: `/buscar` sin parámetro, con `?q=`, con `?q=%20%20` y con `?q=%` muestran ese estado, con un solo `h1` y sin tocar la base.
- [ ] 14. Declarar la metadata de robots de la página de resultados (`noindex`, `follow`). Verificable: test sobre la metadata exportada de la ruta; home, listado y ficha siguen sin `noindex`.
- [ ] 15. Escribir la suite adversarial del buscador (`tests/buscador-adversarial.test.ts`): consulta de miles de caracteres, `q` repetido, `q` como arreglo, `%`/`_`, comillas y `<script>`, emojis y otro alfabeto, y una consulta que coincide con un negocio `en_revision`. Verificable: ninguna produce error; ninguna filtra datos de negocios no publicados; ninguna devuelve el directorio completo.
- [ ] 16. Actualizar `tests/layout.test.ts`: `/buscar` entra a la lista blanca de rutas existentes y la revisión de enlaces se extiende al `action` de los formularios, fallando si apunta a una ruta inexistente. Verificable: la suite pasa con el buscador puesto y falla si se cambia el `action` por una ruta inventada.
- [ ] 17. Revisar a 390px, 768px y 1280px la home con buscador, una página de resultados con varios negocios, una sin resultados y una con consulta vacía: sin scroll horizontal, áreas táctiles ≥44px, el botón de WhatsApp sigue siendo lo más prominente de cada tarjeta y una consulta larguísima no desborda el encabezado. Verificable: inspección en los tres anchos sobre el sitio servido; lo que necesite ojos humanos queda anotado para el PR.
- [ ] 18. Repasar todos los textos nuevos carácter por carácter contra la spec y contra el español mexicano coloquial del proyecto, y confirmar que ningún dato ficticio nuevo del seed se parece a un negocio real de Tizayuca. Verificable: diff de literales contra el delta de spec y revisión del archivo de seed de demostración.

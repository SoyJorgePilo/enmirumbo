# Tareas: preparar-deploy-produccion

- [x] 1. **Spike de la base local** (`design.md` §3): probar `npx prisma dev` y, si no alcanza, un `docker compose` con `postgres:17`. Verificable: contra la base elegida se puede aplicar una migración, correr `npm run db:seed` y ejecutar una prueba que lea la base; queda escrito en el reporte cuál se eligió, con qué comando se levanta y qué forma tiene su `DATABASE_URL`.
  - **Elegido `npx prisma dev`** (`npm run db:local`), PostgreSQL 17.5 sobre PGlite, sin Docker. `DATABASE_URL="postgresql://postgres:postgres@localhost:51214/template1?sslmode=disable"` (puertos fijos). Verificado: `migrate deploy`, `db:seed` (8/21/49) y la suite completa. Dos límites suyos, medidos y documentados en `reports/b-dev.md` y en `docs/despliegue.md` §2: **una sola sesión compartida entre conexiones** (un `SET search_path` se le filtra a todos) y **corte a las 10 conexiones simultáneas**. Los dos moldearon el diseño de la base de pruebas.
- [x] 2. **Pasar el esquema a PostgreSQL y rehacer el árbol de migraciones**: `provider = "postgresql"` en `prisma/schema.prisma`, borrar las migraciones SQLite y generar una sola migración inicial que consolide el modelo actual, con `migration_lock.toml` en `postgresql`. Verificable: sobre una base vacía, `prisma migrate deploy` deja todas las tablas del modelo y `prisma migrate diff` no reporta drift contra el esquema.
  - **Corrección al enunciado:** eran **siete** migraciones SQLite, no tres (el change se escribió antes de que mergearan foto, reportes, despublicación y versión del aviso). La consolidada las cubre a las siete.
- [x] 3. **Reponer a mano los CHECK de `estado` y `origen`** en la migración inicial, con comentario de que son escritos a mano y por qué. Verificable: un `INSERT` directo con `estado = 'inventado'` lo rechaza la base; una prueba nueva lo ejercita después de aplicar el árbol completo.
  - **Corrección al enunciado:** son **cuatro** constraints, no dos: se sumaron `Reporte.motivo` y `Reporte.estado` cuando mergeó T-011. Las cuatro van con nombre explícito (`ALTER TABLE … ADD CONSTRAINT`) para que el error de la base nombre la regla violada.
- [x] 4. **Cambiar el adaptador a `@prisma/adapter-pg`** en los lugares que instancian el cliente y sacar `@prisma/adapter-better-sqlite3` del `package.json`. Verificable: `npm run build` y un arranque local contra la base nueva; ninguna dependencia de SQLite queda en el árbol.
  - **Corrección al enunciado:** eran **cinco** lugares, no cuatro (`prisma/barrer-fotos-huerfanas.ts` llegó con T-008). Los cuatro de `prisma/` se unificaron en `prisma/cliente-script.ts`.
- [x] 5. **Base de pruebas en PostgreSQL**: `tests/global-setup.ts` recrea la base de prueba desde cero y `vitest.config.mts` apunta a ella, conservando `fileParallelism: false`. Verificable: `npm test` en verde con toda la suite, dos corridas seguidas dan lo mismo, y ninguna prueba depende de un archivo `.db`.
- [x] 6. **CI contra Postgres efímero**: `service` `postgres:17` en `.github/workflows/ci.yml`, migraciones, `npm run db:seed` y la suite contra él; el paso de `npm run build` corre **sin** `DATABASE_URL` alcanzable.
- [x] 7. **Guardas de entorno al mundo Postgres** (`prisma/guardas-entorno.ts`): `apuntaABaseLocal` reconoce lo local por host (`localhost`, `127.0.0.1`, `[::1]`) en vez de por `file:`; las políticas de cada script no cambian.
- [x] 8. **Fallar a la vista y no en silencio** (`src/lib/prisma.ts`): fuera de producción se conserva el default local; en producción, sin `DATABASE_URL` el sistema lo dice con el nombre de la variable y no cae a ninguna base por defecto. Aviso de arranque sin `SITIO_URL`, una sola vez por proceso.
  - El aviso de `SITIO_URL` **ya existía** (`avisarSinUrlSitioUnaVez`, llamado en el tronco de `src/app/layout.tsx`): esta tarea le agregó la prueba que vigila que siga siendo al arrancar y no por petición.
- [x] 9. **Prueba de que el build no toca la base**: barrido de `src/app` que exige que toda ruta que consulte la base se renderice por petición, con caso negativo.
- [x] 10. **Lógica de la purga** en `src/lib/purga/`: función pura de la fecha de corte y borrado definitivo con su conteo.
- [x] 11. **Ruta de disparo** de la purga (Route Handler) con `Authorization: Bearer <CRON_SECRET>`; sin secreto o con secreto equivocado, 404 sin borrar nada.
- [x] 12. **Programación diaria** en `vercel.json` más la nota en el documento de cómo dispararla desde cualquier otro programador de tareas.
- [x] 13. **Actualizar `PENDIENTES_OPERATIVOS_LEGALES`** (`src/lib/legales/textos.ts`): sale la purga, entra el pendiente del encargado del tratamiento con su ticket (E6-3). El texto publicado de las dos páginas legales no cambia ni un carácter.
- [x] 14. **Escribir `docs/despliegue.md`** con las cinco partes que pide la spec, más las secciones de fotos y analítica.
- [x] 15. **Prueba que ata el documento al código**: barrido de `src/` y `prisma/` en busca de variables de entorno; cada nombre encontrado debe aparecer en `docs/despliegue.md`, salvo las que pone la plataforma.
- [x] 16. **Actualizar `.env.example`**: `DATABASE_URL` local de Postgres con el comando para levantarla, `CRON_SECRET`, la nota del encabezado de IP y las variables que cambiaron de significado.
- [x] 17. **Pasada final de coherencia**: `openspec/project.md`, `README.md` y `docs/decisiones/README.md` dejan de decir "SQLite (desarrollo)".
  - `CLAUDE.md` **no se tocó a propósito** (es configuración del agente, no la edita el dev): la línea exacta y el texto sugerido van en el reporte para que la cambie el humano.

## Tareas nuevas (encargo del orquestador tras la aprobación)

- [x] 18. **Enmendar los ADRs**: ADR-004 y ADR-007 pasan a "aceptada (ejecutada en E0-3 / T-013)" con la sección de qué se ejecutó; ADR-001 gana una nota de enmienda ("el dev local pasa de SQLite a Postgres local"). Verificable: `docs/decisiones/README.md` refleja los tres estados.
- [x] 19. **Content-Security-Policy** (`src/lib/seguridad/csp.ts` + `next.config.ts`): `script-src` con `cloud.umami.is` y `connect-src` con `gateway.umami.is`, más el cierre de enmarcado, plugins y `form-action`. Documentada en `docs/despliegue.md` §8 con el `curl` para verificarla contra el sitio desplegado.
- [x] 20. **Cron del barrido de fotos huérfanas** con vigilancia del código de salida: ruta `/api/tareas/barrer-fotos-huerfanas` tras el mismo secreto, que responde **500 cuando una salvaguarda detuvo el barrido** (fail-closed: un 200 con letra chica lo daría por bueno y las huérfanas se acumularían en silencio). Declarada en `vercel.json`.
- [x] 21. **Borde para el byte nulo** (no estaba en la spec; lo destapó el cambio de dialecto). PostgreSQL no admite el byte nulo en texto y lo que en SQLite era un dato raro pasaba a ser un error del servidor: una URL con `%00` devolvía 500 en vez de 404. Se filtra en el borde (`src/lib/texto.ts`, usado por `ficha-url`, `admin/reportes` y `reportes/crear`), no cerca de la base.

## Iteración 2 (correcciones tras la etapa C)

- [x] 22. **A1 · El host efectivo, no el de la URL** (`src/lib/base-datos/conexion.ts`): las guardas resuelven el host con `pg-connection-string` —el parser que `pg` tiene debajo—, así que `?host=` ya no engaña a `apuntaABaseLocal`; `hostaddr` y cualquier esquema que no sea `postgres(ql)` se tratan como remotos. Las dos pruebas `[A1]` de la etapa C, en verde.
- [x] 23. **A2 · TLS obligatorio hacia fuera**: si el host efectivo no es local y la dirección no pide cifrado, ni la aplicación ni los comandos de `prisma/` abren la base — lo dicen en el log al arrancar y fallan a la vista. Los literales de `docs/despliegue.md` llevan `sslmode=require` y una prueba barre el documento para que ninguno se quede sin él.
- [x] 24. **A3 · El tope de reportes vuelve a ser atómico**: el `INSERT` condicionado va dentro de una transacción con `pg_advisory_xact_lock(hashtext(negocioId))`. `tests/concurrencia-real.test.ts` abre dos conexiones de verdad y comprueba las dos caras (con cerrojo no se pasa el tope; sin cerrojo sí). **Se salta en la base local** —PGlite multiplexa todo sobre un backend— y corre en el CI contra `postgres:17`.
- [x] 25. **A4 · El límite del panel, a la base** (`src/lib/cupos/compartido.ts`, tabla `IntentoDeCupo`): ventana deslizante, comprobar y apartar en una transacción con cerrojo, clave = HMAC de la IP con el secreto de sesión (nunca la IP), filas borradas al salir de la ventana, y la memoria como respaldo documentado si la base no responde. `docs/despliegue.md` §3.5 corrige la afirmación falsa y dice, límite por límite, cuál se comparte entre instancias.
  - **DESVIACIÓN ARGUMENTADA:** los cupos del formulario público y de los reportes **no** se movieron. El aviso de privacidad ya publicado dice literal que la IP de quien envía el formulario se usa *"solo en su memoria… No la guardamos en la base de datos"*, y este change tiene prohibido tocar el texto legal aprobado. Queda declarado en `PENDIENTES_OPERATIVOS_LEGALES` y en §10 del documento. Detalle en `reports/b-dev.md`.
- [x] 26. **A5 · Adaptador de Supabase Storage** (`src/lib/fotos/almacen-supabase.ts`), detrás del mismo puerto y elegido por variables. El puerto ganó `listar()` y `descripcion()` para que el barrido de huérfanas deje de leer el disco por su cuenta; el borrado ARCO y la purga se llevan los archivos del almacén configurado; un almacén vacío con fichas que dicen tener foto ya no es "nada que barrer". Pruebas con `fetch` simulado detrás del puerto; las llamadas reales, en la prueba de humo (§9, pasos 10 y 11).
- [x] 27. **Medios de la misma vuelta**: 404 de tareas sin marca propia (M1); `try` por registro en la purga, con conteo de fallidos y 500 si no es cero (M3); byte nulo filtrado también en los motivos de rechazo y de despublicación (M4); aviso al arrancar si falta `CRON_SECRET` (M5); la contraseña de producción fuera de la línea de comandos en el documento (M6); colación de la base local documentada (M2); `?pgbouncer=true` explicado (B4); comentario de la migración apuntando al archivo que existe (B5); límite de 2 crons del plan Hobby (B6).
- [x] 28. **Enmiendas de spec** (M8): el requirement de fotos se reescribe —el sistema SÍ acepta y sirve fotos—, el scenario "migración sobre una base con datos" se enuncia en los términos verificables del árbol consolidado, y entran los cuatro requirements nuevos de esta iteración (TLS, cupos compartidos, tope atómico, 404 sin marca).
- [x] 29. **Adenda del orquestador · cabeceras de seguridad globales**: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY` (junto a `frame-ancestors 'none'` de la CSP, a propósito: una para navegadores modernos y otra para los viejos), `Referrer-Policy: strict-origin-when-cross-origin` y `poweredByHeader: false`, todo en el mismo mecanismo de la CSP. Verificado sobre respuestas reales (dinámica, estática y 404) y comprobado que la meta `strict-origin` del panel sigue mandando sobre la cabecera global. `Strict-Transport-Security` NO se manda desde la aplicación —la pone el hosting con su certificado—: queda como paso humano en §10.
- [x] 30. **Adenda del orquestador · deuda de vigilancia de dependencias**: `docs/despliegue.md` §10 documenta las 4 altas de `npm audit` (deepmerge-ts y mysql2 por `@prisma/config`), por qué no son explotables en el runtime (`prisma` es `devDependency`; lo que se despliega es `@prisma/client` + `@prisma/adapter-pg`) y por qué **no** se corre `npm audit fix --force`: instalaría Prisma 6 y con él se pierde el adaptador de driver sobre el que está montado el change.

## Iteración 3 (última, tras la re-auditoría §7)

- [x] 31. **R1 · Las marcas del cupo caducan de verdad** (`limpiarCuposCaducados`, llamada desde la tarea diaria de purga): borra lo que salió de la retención —una hora, con un invariante que exige que sea mayor que la ventana más larga en uso— y **poda por techo de filas** (`MAX_FILAS_DE_CUPOS = 5000`, paridad con el `MAX_IPS_RASTREADAS` de la versión en memoria, y más estricto porque cuenta filas y no IPs). La purga informa `cuposLimpiados`. Corregidas las dos frases que prometían lo que no pasaba (migración y `docs/despliegue.md` §3.5) y la del `schema.prisma`. Test `[R1]` de la etapa C, en verde.
- [x] 32. **R2 · El disco efímero ya no vuelve en silencio**: con el sistema desplegado —producción, o base que no está en esta máquina— y sin las variables de Supabase, el almacén es uno que **falla a la vista** (`guardar` y `listar` lanzan; `leer` devuelve nada y `borrar` se completa sin mentir, porque no hay archivo), con aviso al arrancar junto a los de `SITIO_URL`, `DATABASE_URL` y `CRON_SECRET`. En la máquina de alguien sigue siendo el disco local. Test `[R2]`, en verde.
- [x] 33. **R3 · El tratamiento del panel, declarado**: cuarto renglón en `PENDIENTES_OPERATIVOS_LEGALES` con qué se guarda (HMAC, nunca la IP), para qué (art. 19 LFPDPPP: proteger la única credencial), cuánto dura (la ventana; nada pasa de una hora **ahora que R1 lo hace cierto**) y a quién alcanza (cualquiera que envíe el formulario de `/admin`, que es una página pública). Ticket E6-3. No toca ningún texto publicado.
- [x] 34. **B7 · `sslmode=prefer` deja de contar como cifrado** (también `allow` y `disable`), adelantándose a la semántica de libpq que `pg` v9 adoptará, y el mensaje explica que el modo que trae no basta.
- [x] 35. **B8 · El socket Unix tiene salida**: no se le exige TLS —los bytes no salen de la máquina— y el mensaje de conexión insegura dice cómo escribirlo. **Decisión declarada:** un socket sigue sin contar como "base local" para los comandos que escriben en masa, porque de una ruta no se sabe a qué servidor lleva; la salida es el permiso explícito. Documentado en §3.4.

## Iteración 4 (decisión del fundador sobre R4)

- [x] 36. **R4 · El borrado se niega a mentir** (opción (a), decidida por el fundador). `borrarNegocioDefinitivamente` **invierte el orden**: si la ficha tiene `fotoClave`, primero se llevan los archivos y sólo entonces se borra la fila; si el almacén no se deja alcanzar, **la fila no se toca** y devuelve `"almacen-inalcanzable"`. El panel lo dice con un literal propio ("La ficha no se borró: no pude alcanzar el almacén de fotos. Revisa la configuración y vuelve a intentar."), la purga diaria lo cuenta en `fallidos` —que ya hace responder 500 al cron— y lo reintenta mañana. Una ficha SIN foto se borra normal aunque el almacén esté caído. `crearAlmacenSinConfigurar().borrar` pasó a lanzar, porque desde ahí no se puede saber si el almacén nunca estuvo configurado o si se perdió la configuración con las fotos dentro. Test `[R4]` en verde y su compañero invertido a fijar el comportamiento nuevo.

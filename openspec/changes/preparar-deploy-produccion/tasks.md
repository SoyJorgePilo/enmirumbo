# Tareas: preparar-deploy-produccion

- [ ] 1. **Spike de la base local** (`design.md` §3): probar `npx prisma dev` y, si no alcanza, un `docker compose` con `postgres:17`. Verificable: contra la base elegida se puede aplicar una migración, correr `npm run db:seed` y ejecutar una prueba que lea la base; queda escrito en el reporte cuál se eligió, con qué comando se levanta y qué forma tiene su `DATABASE_URL`.
- [ ] 2. **Pasar el esquema a PostgreSQL y rehacer el árbol de migraciones**: `provider = "postgresql"` en `prisma/schema.prisma`, borrar las tres migraciones SQLite y generar una sola migración inicial que consolide el modelo actual (T-001 + rastro de rechazo + columnas normalizadas de búsqueda), con `migration_lock.toml` en `postgresql`. Verificable: sobre una base vacía, `prisma migrate deploy` deja todas las tablas del modelo y `npx prisma migrate status` no reporta drift contra el esquema.
- [ ] 3. **Reponer a mano los CHECK de `estado` y `origen`** en la migración inicial, con comentario de que son escritos a mano y por qué. Verificable: un `INSERT` directo con `estado = 'inventado'` lo rechaza la base; una prueba nueva lo ejercita después de aplicar el árbol completo (`modelo-datos`, scenarios "valores fuera del conjunto" y "las constraints sobreviven a todo el árbol").
- [ ] 4. **Cambiar el adaptador a `@prisma/adapter-pg`** en los cuatro lugares que instancian el cliente (`src/lib/prisma.ts`, `tests/db.ts`, `prisma/seed-demo.ts`, `prisma/backfill-busqueda.ts`) y sacar `@prisma/adapter-better-sqlite3` del `package.json`. Verificable: `npm run build` y un arranque local contra la base nueva; ninguna dependencia de SQLite queda en el árbol.
- [ ] 5. **Base de pruebas en PostgreSQL**: `tests/global-setup.ts` recrea la base de prueba desde cero (esquema limpio + `migrate deploy`) y `vitest.config.mts` apunta a ella, conservando `fileParallelism: false`. Verificable: `npm test` en verde con toda la suite actual, dos corridas seguidas dan lo mismo, y ninguna prueba depende de un archivo `.db`.
- [ ] 6. **CI contra Postgres efímero**: agregar el `service` `postgres:17` a `.github/workflows/ci.yml`, aplicar migraciones, correr `npm run db:seed` y correr la suite contra él; el paso de `npm run build` corre **sin** `DATABASE_URL` alcanzable. Verificable: el workflow pasa en el PR; si se le mete a propósito una migración con SQL inválido para Postgres, el CI falla en ese paso.
- [ ] 7. **Guardas de entorno al mundo Postgres** (`prisma/guardas-entorno.ts`): `apuntaABaseLocal` reconoce lo local por host (`localhost`, `127.0.0.1`, `[::1]`) en vez de por `file:`; las políticas de cada script no cambian. Verificable: pruebas de `motivoParaNoSembrar`/`motivoParaNoRellenar` con `VERCEL_ENV=production`, con una URL remota sin permiso, con una URL remota con permiso y con la URL local.
- [ ] 8. **Fallar a la vista y no en silencio** (`src/lib/prisma.ts`): fuera de producción se conserva el default local; en producción, sin `DATABASE_URL` el sistema lo dice con el nombre de la variable y no cae a ninguna base por defecto. Sumar el aviso de arranque cuando falta `SITIO_URL` en producción, una sola vez por proceso (mismo patrón que `avisarSinConfigurarUnaVez`). Verificable: pruebas de las cuatro combinaciones (producción/no producción × variable presente/ausente) y que el aviso no se repite por petición.
- [ ] 9. **Prueba de que el build no toca la base**: barrido de `src/app` que exige que toda ruta que consulte la base se renderice por petición, con caso negativo. Verificable: la suite falla si se agrega una página que lee la base sin optar por render dinámico; `next build` completa sin base accesible.
- [ ] 10. **Lógica de la purga** en `src/lib/purga/`: función pura que, dada la fecha de corte, selecciona los rechazados con 90 días o más, y el borrado definitivo con su conteo. Verificable: pruebas contra la base de 89 días (no), 90 días (sí), 91 días (sí), rechazado sin fecha (no), publicado y en revisión (nunca), rechazado que volvió a la cola (no), y segunda corrida que informa cero.
- [ ] 11. **Ruta de disparo** de la purga (Route Handler): exige `Authorization: Bearer <CRON_SECRET>`; sin secreto configurado o con secreto equivocado responde 404 sin borrar nada; la respuesta y el log llevan solo el conteo; la ruta no es indexable. Verificable: pruebas adversariales (sin encabezado, con secreto equivocado, con secreto vacío, con el sistema sin secreto configurado) y una con el secreto correcto que sí purga.
- [ ] 12. **Programación diaria** en `vercel.json` (una corrida de madrugada) más la nota en el documento de cómo dispararla desde cualquier otro programador de tareas. Verificable: el archivo declara la ruta y la frecuencia; el documento trae el `curl` equivalente.
- [ ] 13. **Actualizar `PENDIENTES_OPERATIVOS_LEGALES`** (`src/lib/legales/textos.ts`): sale la purga de los 90 días, entra el pendiente de nombrar al encargado del tratamiento (ADR-004) con su ticket. Verificable: `tests/legales-borrador.test.ts` (o equivalente) refleja los dos pendientes vigentes; el texto publicado de las dos páginas legales no cambia ni un carácter.
- [ ] 14. **Escribir `docs/despliegue.md`** con las cinco partes que pide la spec: variables (obligatorias/opcionales, con `REGISTRO_ENCABEZADO_IP = x-forwarded-for` literal y las dos URLs de base si el spike confirmó que hacen falta), orden de operaciones, pasos humanos (cuentas, dominio, DNS, respaldos), lo que nunca se corre en producción, y la verificación de humo. Incluir las secciones de fotos (Supabase Storage, ADR-006, la implementa T-008) y analítica (cuenta de Umami, las variables las define T-010). Verificable: una persona ajena puede seguirlo de arriba abajo sin abrir el código.
- [ ] 15. **Prueba que ata el documento al código** (`design.md` §8): barrido de `src/` y `prisma/` en busca de lecturas de `process.env`; cada nombre encontrado debe aparecer en `docs/despliegue.md`, salvo la lista explícita de las que pone la plataforma. Verificable: agregar una variable inventada al código rompe la suite; documentarla la arregla.
- [ ] 16. **Actualizar `.env.example`**: `DATABASE_URL` local de Postgres con el comando para levantarla, `CRON_SECRET`, la nota del encabezado de IP en Vercel y las variables que cambiaron de significado. Verificable: `cp .env.example .env` + el comando de la base + migraciones + seed + `npm run dev` funciona en un clon limpio.
- [ ] 17. **Pasada final de coherencia**: `openspec/project.md` deja de decir "SQLite (desarrollo)"; el PR propone (sin editarlos) la enmienda de ADR-001 y el paso de ADR-004/ADR-007 a "aceptada", y señala la línea de `CLAUDE.md` que menciona SQLite para que la cambie el humano. Verificable: el PR lista esos tres documentos con el texto sugerido y ninguna búsqueda de "SQLite" en `openspec/` devuelve algo que ya no sea cierto.

# Tareas: agregar-enlace-de-gestion

Orden por dependencia: modelo → token → modo edición → panel → ficha pública → repaso. Cada tarea se puede completar y comprobar por sí sola.

## Modelo y migración

- [x] 1. Sustituir en el modelo `Negocio` la columna `tokenGestion` por `tokenGestionHash` (texto, opcional, único) y `tokenGestionCreadoEn` (fecha, opcional), con su migración de Prisma y el cliente regenerado. Verificable: la migración se aplica sobre una base con negocios en los tres estados sin perder filas, los campos nuevos quedan nulos en todas, y dos filas no pueden compartir huella.
- [x] 2. Modelar `EdicionPendiente` (una columna por campo editable del formulario, `negocioId`, `estado` con CHECK `pendiente | aplicada | descartada`, `creadaEn`, `resueltaEn`, `motivoDescarte`), con borrado en cascada desde el negocio y **índice único parcial** que impide dos `pendiente` del mismo negocio (`design.md` §1). Verificable: test que intenta crear la segunda pendiente y falla en la base, y test del CHECK con un estado inventado.
- [x] 3. Escribir el test guardián de campos editables: una sola lista declarada en código enumera qué se puede editar, y el test falla si esa lista y las columnas de `EdicionPendiente` se desincronizan, o si la lista incluye `estado`, `origen`, giros, `publicadoEn`, `registradoEn`, `consintioAvisoEn` o la huella del enlace. Verificable: agregar a mano un campo prohibido a la lista pone la suite en rojo.
- [x] 4. Extender el borrado definitivo del negocio (ARCO) para que se lleve también todas sus ediciones. Verificable: test que borra un negocio con una edición pendiente y dos resueltas y comprueba que ninguna consulta devuelve su nombre, WhatsApp, teléfono ni dirección.

## El token

- [x] 5. Escribir `src/lib/gestion/token.ts`: generación con `crypto.randomBytes` de 32 bytes en base64url, huella SHA-256, y búsqueda por huella con confirmación en tiempo constante (`crypto.timingSafeEqual`), más el armado de la URL absoluta del enlace reutilizando `SITIO_URL` (`design.md` §3). Verificable: tests de que dos generaciones nunca coinciden, de que la huella no permite recuperar el token, de que un token alterado en un carácter no resuelve, y de que la función de búsqueda no compara con `===`.
- [x] 6. Generar el token dentro de la transición de aprobación existente, condicionada a que el registro siga en `en_revision`, sin tocar nada más de lo que ya hace. Verificable: tests de que aprobar deja huella y fecha puestas, de que dos aprobaciones distintas dan tokens distintos, y de que una aprobación repetida no cambia la huella ya guardada.
- [x] 7. Reservar el segmento `editar` en `src/lib/rutas-reservadas.ts`, para que ningún slug de categoría pueda taparlo. Verificable: el test de rutas reservadas falla si se agrega una categoría con ese slug.

## Modo edición (público)

- [x] 8. Crear `src/lib/gestion/textos.ts` con todos los literales del modo edición de la spec ("Edita tu ficha", la frase de acompañamiento, "Enviar cambios", la nota del aviso de privacidad, el aviso de cambios pendientes, la confirmación, "Ese número ya está en otra ficha del directorio.", el cupo y la falla al guardar). Verificable: tests que comparan carácter por carácter contra los textos de la spec. **[a-ui] Literales listos. [b-dev] Tests en `tests/gestion-textos.test.ts`, carácter por carácter.**
- [x] 9. Dar modo edición a `FormularioRegistro` por props (valores iniciales, texto del botón, presencia del bloque de consentimiento), sin duplicar el componente ni la validación (`design.md` §5). Verificable: test de que el formulario renderiza prellenado, sin checkbox de consentimiento y con "Enviar cambios", y de que el registro sigue funcionando exactamente igual. **[a-ui] Props `modo`/`accion`/`textoBoton`. [b-dev] Ratificada la decisión de ocultar la foto en modo edición (la lista de campos editables de `revision-admin` no la incluye) y probada en `tests/gestion-edicion.test.ts`; el registro sigue igual (su suite no se tocó).**
- [x] 10. Construir la página `/editar/[token]` (Server Component): resuelve por huella, prellena con lo publicado, y responde el 404 del sitio para token inexistente, invalidado, alterado o de negocio no publicado. Verificable: tests de los cuatro casos de 404 (misma página y mismo código que una URL inventada) y del caso feliz con todos los campos puestos, incluida la colonia "Otra" con su texto libre. **[b-dev] Mock retirado: resuelve por huella con `timingSafeEqual` (`src/lib/gestion/consultas.ts`). Los cinco casos de 404 (inventado, sin forma, alterado, invalidado, no publicado, borrado) en `tests/gestion-edicion.test.ts`.**
- [x] 11. Cerrarle las fugas al token en la URL (`design.md` §4): `noindex, nofollow` y `referrer: no-referrer` en la página de edición y en su confirmación, sin enlaces externos, y ninguna escritura del token en el log. Verificable: tests de la metadata de ambas pantallas, de que ninguna página pública enlaza a `/editar/`, y un test que espía `console.*` en las rutas feliz y de error buscando el token. **[b-dev] Lista blanca de `tests/buscador-pagina.test.ts` actualizada; test del espía de `console.*` con el token, y el guardián de privacidad comprueba que ninguna superficie pública enlaza a `/editar/`.**
- [x] 12. Implementar la Server Action de envío: valida con las mismas reglas del registro, comprueba que el WhatsApp propuesto no tenga otra ficha, crea la edición pendiente **reemplazando** la anterior en la misma transacción, y redirige a la confirmación (POST-Redirect-GET). Verificable: tests de envío válido (la ficha publicada no cambia ni un campo), de cada mensaje de error reutilizado, del número tomado por otra ficha, y de que recargar la confirmación no crea otra edición. **[b-dev] `src/lib/gestion/procesar-edicion.ts` (regla) + la acción (solo IP y redirección), como el registro.**
- [x] 13. Blindar el envío contra campos no confiables: ignorar cualquier `estado`, `origen`, giros, `publicadoEn`, `registradoEn`, `consintioAvisoEn` o token que venga del cliente, y rechazar un envío cuyo token no corresponda al negocio que dice editar. Verificable: test adversarial que manda todos esos campos y comprueba que la edición guardada solo trae campos capturables y que el negocio no cambia al aplicarla.
- [x] 14. Implementar el caso de la edición pendiente: prellenar con lo último que mandó el dueño y mostrar "Ojo: ya tienes cambios esperando revisión. Si mandas otros, estos reemplazan a los anteriores." Verificable: tests del prellenado desde la pendiente, del reemplazo (queda una sola y con el reloj reiniciado) y de dos envíos casi simultáneos que dejan exactamente una.
- [x] 15. Agregar el anti-abuso propio del envío de ediciones: campo trampa y cupo de 3 por hora y por IP con `crearCupoPorIp`, en un contador separado del de altas y del de acceso al panel. Verificable: tests del cuarto envío bloqueado con su literal, del honeypot que finge éxito sin guardar, y de la independencia entre los tres cupos.
- [x] 16. Recalcular las versiones normalizadas de búsqueda al aplicar una edición, con la misma función que usa el registro. Verificable: test de que tras aplicar una edición que renombra a "Plomería Güicho" la búsqueda de "plomeria" lo encuentra.

## Panel del admin

- [x] 17. Sumar a `src/lib/admin/textos.ts` los literales nuevos (etiquetas "Alta nueva"/"Edición", "Cambios por revisar", "Lo que está publicado", "Lo que quiere cambiar", "Cambió", la advertencia del cambio de WhatsApp, "Aplicar los cambios", "Listo, la ficha ya se actualizó.", "Ese número ya está en otra ficha: no se pudieron aplicar los cambios.", "¿Por qué no aplicas los cambios?", "Descartar los cambios", "Escribe por qué descartas los cambios", "Cambios descartados.", los dos mensajes de concurrencia, "Generar un enlace nuevo", "Listo, el enlace anterior ya no sirve." y "Mandarle el enlace por WhatsApp") y las tres plantillas de WhatsApp nuevas, más la de publicación modificada con el enlace de gestión. Verificable: tests que comparan carácter por carácter contra la spec, incluidas las interpolaciones de nombre, motivo, link de ficha y enlace de gestión. **[b-dev] Enchufada en `aprobado/page.tsx` (con respaldo a la versión sin enlace cuando el sobre caducó). Tests en `tests/gestion-textos.test.ts` y `tests/gestion-panel.test.ts`.**
- [x] 18. Mezclar altas y ediciones en la consulta de la cola, ordenadas por antigüedad de entrada, con su tipo y su marca de 48 horas calculada contra un momento inyectable. Verificable: tests con altas y ediciones de 3, 47, 49 y 200 horas que comprueban orden, etiqueta, marca y conteo, y que un negocio publicado con edición pendiente aparece una sola vez y como "Edición". **[b-dev] `obtenerColaDeRevision` mezcla altas y ediciones; `tipo` y `hrefDetalle` son parte de `RegistroColaItem` (no props opcionales de la tarjeta), así que un renglón nuevo no puede olvidarse de decir qué es.**
- [x] 19. Pintar la cola mezclada con las etiquetas de texto y la entrada "Revisar" hacia el detalle que corresponda, conservando el estado vacío "No hay registros esperando. Todo al día." Verificable: tests de cola con los dos tipos, con un atrasado de cada tipo y vacía. **[b-dev] Enchufado: `/admin/cola` pinta la cola mezclada sin cambiar su código (la tarjeta recibe el item entero).**
- [x] 20. Construir el detalle comparativo de la edición en `/admin/ediciones/[id]`: "Lo que está publicado" vs "Lo que quiere cambiar", marca "Cambió" solo en los campos distintos, advertencia y botón de verificación al número propuesto cuando el WhatsApp cambia, y no encontrado para un identificador inexistente. Verificable: tests de una edición que cambia dos campos, de una que cambia el WhatsApp y de un identificador inventado. **[b-dev] Consulta real en `src/lib/admin/ediciones.ts`: la comparación se calcula al mirar, campo contra campo, contra lo que la ficha dice HOY.**
- [x] 21. Implementar la acción de aplicar la edición: copia campo a campo por lista blanca, condicionada a que esa edición siga siendo la pendiente de ese negocio, revalidando la unicidad del WhatsApp; deja intactos estado, origen, giros, fechas, consentimiento y enlace. Verificable: tests de aplicación exitosa (comparando el negocio antes y después), del número tomado por otra ficha y de que ningún campo no editable se mueve. **[b-dev] `aplicarEdicion` copia por lista blanca dentro de una transacción condicionada, revalida la unicidad del WhatsApp y recalcula el texto del buscador.**
- [x] 22. Implementar la acción de descartar la edición con motivo obligatorio, guardando motivo y fecha, sin tocar la ficha ni el enlace. Verificable: tests de descarte con motivo, de descarte sin motivo (no cambia nada) y de que el enlace sigue abriendo el formulario después. **[b-dev] El motivo se GUARDA y la confirmación lo lee de la fila, nunca de la URL. Cota de 500 caracteres, rechazada y no recortada (viaja por WhatsApp).**
- [x] 23. Agregar el manejo de concurrencia a las dos acciones: "Estos cambios ya los habías resuelto." si ya estaba resuelta y "Estos cambios ya no son los últimos: el negocio mandó otros más nuevos." si fue reemplazada. Verificable: tests de doble aplicación, de doble descarte y del reemplazo entre que el admin abre y resuelve. **[b-dev] Mecanismo real: `updateMany` con `id` + `estado: 'pendiente'`; si no afecta filas, se distingue "reemplazada" (hay otra pendiente más nueva) de "ya resuelta". El atajo por `searchParams` ya solo PINTA el aviso que decidió la escritura.**
- [x] 24. Agregar las pantallas de confirmación de aplicar y descartar con sus botones "Avisarle por WhatsApp" y los mensajes prellenados; recargar no repite la acción. **[b-dev] Las dos confirmaciones exigen el estado ya persistido (`aplicada`, y `descartada` CON motivo): abrirlas a pelo o recargarlas no repite nada y no anuncia lo que no pasó.**
- [x] 25. Implementar "Generar un enlace nuevo" en el detalle del negocio publicado... **[b-dev] Enchufado en el detalle de una ficha publicada. `regenerarEnlaceDeGestion` sobrescribe la huella condicionada al estado; el enlace en claro viaja en el sobre de un solo uso (`src/lib/gestion/sobre.ts`), nunca por la URL.**
- [x] 26. Modificar la pantalla de confirmación de la aprobación para que el mensaje de WhatsApp lleve también el enlace de gestión con la instrucción del PRD §6.4, y para que el enlace no quede visible fuera de ella. Verificable: test del mensaje completo con los dos links y test de que volver al detalle ya no lo muestra. **[a-ui] No enchufado a propósito: rompería el marcador "sin enlace de gestión todavía" de `tests/admin-textos.test.ts` y `tests/admin-paginas.test.ts`. La función de texto ya existe (tarea 17) — ver reports/a-ui.md §4.3.**
- [x] 27. Comprobar que la guarda de sesión cubre todas las rutas y acciones nuevas del panel. **[b-dev] Tests de que sin sesión no se publica, no se genera enlace, no se aplica, no se descarta y no se regenera — comprobado contra la base, no solo contra la redirección.**

## Ficha pública

- [x] 28. Agregar `WHATSAPP_ADMIN` a `.env.example` con su comentario de fail-safe (sin ella no se pinta el botón) y su lectura en el código, sin número por defecto. Verificable: test que falla si alguna línea activa del archivo le asigna valor y si el código le pone un `??` con un número. **[a-ui] Hecho. [b-dev] Test en `tests/gestion-textos.test.ts`: ninguna línea activa de `.env.example` le asigna valor y el código no le pone un número de respaldo.**
- [x] 29. Agregar a la ficha el bloque "¿Es tu negocio?" con el control "Perdí mi enlace" y su mensaje prellenado hacia el admin, en jerarquía menor que "Enviar WhatsApp" y solo en la ficha. Verificable: tests del enlace `wa.me` con el mensaje codificado, de la ausencia total del bloque sin la variable configurada, y de que no aparece en tarjetas de listado ni de resultados. **[a-ui] Hecho. [b-dev] Literales y `wa.me` cubiertos en `tests/gestion-textos.test.ts`; la ausencia del bloque sin variable la comprueba `tests/gestion-privacidad.test.ts` (corre sin `WHATSAPP_ADMIN`).**
- [x] 30. Escribir el test de privacidad de las ediciones: con un negocio publicado que tiene enlace y una edición pendiente que cambia nombre, horario y teléfono, ninguna superficie pública (home, listado, filtro, ficha, resultados de búsqueda) muestra ni un valor propuesto, ni la huella, ni una URL de edición, ni ninguna señal de que hay cambios esperando. Verificable: la suite corre contra el sitio servido y busca esos valores en el HTML.

## Repaso final

- [x] 31. Actualizar `tests/layout.test.ts` para que la revisión de enlaces acepte las rutas nuevas (`/editar/<token>`, `/editar/<token>/…`, `/admin/ediciones/<id>`) sin dejar de fallar ante enlaces internos inexistentes, y para que siga exigiendo `rel="noopener noreferrer"` en los `wa.me` nuevos. Verificable: la suite pasa con las pantallas nuevas y falla si se agrega un href interno inventado.
- [~] 32. Revisar a 390px y 1280px las pantallas nuevas (modo edición, confirmación, cola mezclada, detalle comparativo, confirmaciones del panel y el bloque de la ficha): sin scroll horizontal, áreas táctiles ≥44px, contraste AA y la comparación legible sin desplazarse a los lados. Verificable: inspección en ambos anchos sobre el sitio servido; lo que no se pueda comprobar automáticamente queda anotado para el humano del PR. **[b-dev] La parte automatizable está cubierta: el modo edición y su confirmación entraron a `tests/responsivo-guardian.test.ts` (clases que impiden colapsar, retículas sin punto de quiebre) y `src/components/gestion` al recorrido de fuentes públicas. La inspección visual a 390px/1280px de las pantallas del PANEL queda para la etapa de seguridad/test y para el humano del PR — el guardián solo cubre superficies públicas.**
- [x] 33. Repasar todos los textos nuevos contra los literales de la spec y contra el español mexicano coloquial del proyecto, y confirmar que ningún dato de fixtures ni de seeds se parece a un negocio o a un número real. Verificable: diff de textos contra la spec y revisión del HTML servido. **[b-dev] `tests/gestion-textos.test.ts` compara los 33 literales carácter por carácter. Fixtures nuevas: series 771000 5xxx/6xxx/7xxx/9xxx, todas con nombres marcados como ficticios; el seed no siembra ninguna edición.**
- [x] 34. Coordinar con el change `agregar-boton-reportar` si ya está mergeado: los dos agregan un control discreto al pie de la ficha ("Reportar este negocio" y "Perdí mi enlace") y su convivencia visual y de jerarquía tiene que resolverse en un solo lugar. Verificable: la ficha con los dos controles se ve ordenada a 390px y "Enviar WhatsApp" sigue siendo la única acción principal. **[a-ui] `agregar-boton-reportar` ya estaba mergeado (existe `boton-reportar.tsx`). Los dos controles comparten un solo separador al pie de la ficha; "Enviar WhatsApp" sigue siendo lo primero y lo más grande.**

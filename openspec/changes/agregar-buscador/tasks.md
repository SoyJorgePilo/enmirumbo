# Tareas: agregar-buscador

- [x] 1. Extraer de `src/lib/slug.ts` el quitado de acentos a un helper compartido (NFD + borrado de marcas combinantes), sin cambiar el comportamiento de `slugify`. Verificable: `tests/slug.test.ts` sigue en verde sin tocar sus casos. — **b-dev**: `src/lib/texto.ts` (`quitarAcentos`), usado por `slug.ts` y `busqueda.ts`; import relativo porque `prisma/seed.ts` lo carga con `tsx`. `tests/slug.test.ts` intacto y en verde.
- [x] 2. Crear `src/lib/busqueda.ts` con la normalización de texto (`normalizarTexto`: sin acentos, minúsculas, todo lo que no sea letra o dígito a espacio, espacios colapsados) y el troceo de la consulta en raíces (`terminosDeBusqueda`: recorte a 60 caracteres, máximo 4 términos, se descartan los de un solo carácter, raíz de 5 letras para los términos de 5 o más). Sin dependencias nuevas (`design.md` §2). Verificable: tests unitarios con acentos, "ñ", mayúsculas, signos, `%` y `_`, emojis y otro alfabeto, cadena de miles de caracteres, muchos términos, término de una letra y consulta de puros espacios (todas devuelven lista vacía o acotada, ninguna truena). — **b-dev**: `tests/busqueda.test.ts` (27 casos con `tests/slug.test.ts`). Sin dependencias nuevas. — **b-dev (iteración 2)**: por los hallazgos M-2 y M-3 de la etapa C, el tope de 60 se aplica al texto YA normalizado (antes se aplicaba a la cadena cruda, y el relleno se comía la consulta) y se descartan las muletillas de la pregunta antes del tope de 4 términos.
- [x] 3. Agregar a `prisma/schema.prisma` las columnas `nombreNormalizado` y `queOfrecesNormalizado` (texto, no nulas, default `""` solo para poder migrar filas existentes) y generar la migración. Verificable: una base desde cero migra y hace `npm run db:seed` sin error; el cliente de Prisma expone los dos campos. — **b-dev**: migración `20260904032104_agrega_texto_normalizado_de_busqueda`. **Se reescribió a mano** el SQL que generó Prisma: su redefinición de tabla (patrón de SQLite) borraba los CHECK de `estado` y `origen` de la migración inicial; se usa `ALTER TABLE ADD COLUMN`, que los conserva (`npx prisma migrate status` sin drift).
- [x] 4. Escribir en `src/lib/busqueda.ts` la función única `datosDeBusqueda(nombre, queOfreces)` que devuelve el par de valores normalizados listos para guardar, y usarla en el alta del registro (`src/lib/registro/`). Verificable: un alta con "Plomería Güicho" queda con `nombreNormalizado` "plomeria guicho"; un alta sin "¿Qué ofreces?" queda con cadena vacía, no nula; un envío que manda esos campos extra no los altera. — **b-dev**: `src/lib/registro/procesar.ts` paso 5; `tests/busqueda-datos.test.ts`.
- [x] 5. Crear el script de relleno idempotente (`npm run db:backfill:busqueda`) que recalcula las dos columnas de todos los negocios guardados y avisa cuántos actualizó. Verificable: sobre una base con filas previas (columnas en `""`), la primera corrida las llena y la segunda no cambia nada ni toca otros campos. — **b-dev**: `prisma/backfill-busqueda.ts`; probado también a mano contra `prisma/dev.db` (dos corridas seguidas). — **b-dev (iteración 2)**: por el hallazgo B-1 lleva guarda de entorno (`motivoParaNoRellenar` + `BACKFILL_PERMITIR=1`), compartiendo con el seed el criterio de `prisma/guardas-entorno.ts`.
- [x] 6. Actualizar `prisma/seed-demo.ts`: escribir las columnas normalizadas con `datosDeBusqueda` y asignar giros del catálogo a un par de negocios publicados, incluido uno cuyo giro NO aparezca en su nombre ni en su "¿Qué ofreces?" (p. ej. la fonda con el giro "Comida corrida") y conservando al menos un negocio con acentos en sus palabras clave. Idempotente también en los giros (`design.md` §3). Verificable: correrlo dos veces no cambia el conteo de negocios ni duplica vínculos de giro; `npm run db:seed` sigue dejando la tabla de negocios vacía. — **b-dev**: giros en la plomería (`plomeria`), la fonda (`fonda-comida-corrida`, el caso invisible), la academia (`futbol`) y el club de natación (`natacion`); `queOfreces` de la academia pasó a "Fútbol" con acento a propósito. Idempotencia por `set` al actualizar / `connect` al crear.
- [x] 7. Escribir el test de consistencia de las columnas normalizadas: para cada negocio de la base de pruebas, el valor guardado es exactamente `datosDeBusqueda` de sus campos fuente. Verificable: la suite falla si se crea un negocio por un camino que no usa esa función (la red que protege al buscador de futuras escrituras, `design.md` §1). — **b-dev**: `tests/busqueda-datos.test.ts` (incluye el caso negativo: una fila creada a mano se señala por nombre).
- [x] 8. Agregar `buscarNegociosPublicados(consulta)` a `src/lib/directorio.ts`, con `estado: publicado` por construcción como el resto del módulo, exigiendo todos los términos (cada uno contra nombre normalizado, "¿Qué ofreces?" normalizado o el `slug` de sus giros), orden `publicadoEn desc` + `nombre asc`, y la misma proyección de campos públicos que el listado (sin las columnas normalizadas). Verificable: tests contra la base de pruebas de "plomero" → el de "plomería" y el de "plomeria"; "futbol" → el club de "fútbol"; "pinatas" → el de "piñatas"; búsqueda por giro que no está en nombre ni palabras clave; dos términos exigidos ambos; un `en_revision` y un `rechazado` que coinciden y nunca vuelven; consulta sin términos → lista vacía sin tocar la base. — **b-dev**: `tests/busqueda-consultas.test.ts` (21 casos, con fixtures propias `7719998xxx`); "sin tocar la base" verificado con un espía sobre `negocio.findMany`.
- [x] 9. Crear el componente del buscador en `src/components/directorio/` (Server Component, sin directiva de cliente): formulario GET hacia `/buscar` con `input` de tipo búsqueda, etiqueta visible asociada "Busca lo que necesitas", ejemplo dentro del campo "ej. plomero, tacos, futbol infantil", botón "Buscar", ambos con área táctil ≥44px, y valor inicial opcional para poder prellenarlo. Verificable: se renderiza aislado en test con y sin valor inicial; el HTML trae los tres literales y el `action` a `/buscar`. — **a-ui**: `src/components/directorio/buscador.tsx`, verificado manualmente (curl) en home y `/buscar`; falta el test aislado (dev/seguridad-test). — **b-dev**: test aislado en `tests/buscador-pagina.test.ts` (literales, `for`/`id`, prellenado, sin encabezado propio, sin JS).
- [x] 10. Insertar el buscador en la home (`src/app/page.tsx`) arriba de la sección "Busca por categoría", sin encabezado propio. Verificable: el HTML de la home sigue con exactamente un `h1` y tres `h2`, el formulario aparece antes del encabezado "Busca por categoría", y `tests/layout.test.ts` sigue en verde. — **a-ui**: hecho. — **b-dev**: además la grilla de categorías de la home se unificó con `CategoriasGrid` (el mismo marcado que ofrece `/buscar`), y los dos scenarios preexistentes que prohibían `<form>` en la home se actualizaron conforme al delta (ver #16).
- [x] 11. Crear `src/app/buscar/page.tsx` (Server Component, lectura por request): lee el parámetro de consulta (primer valor si viene repetido), muestra el buscador prellenado, el `h1` `Resultados para "<consulta recortada>"` y las tarjetas de `TarjetaNegocio` con el mismo enlace de ficha y de WhatsApp que el listado. Verificable: `/buscar?q=plomero` lista los negocios sembrados que coinciden, en el orden esperado, y la consulta se muestra escapada como texto (`<b>plomero</b>` no genera marcado). — **a-ui**: página creada contra mocks. — **b-dev**: mock borrado (`src/lib/mock/` ya no existe) y conectada a `terminosDeBusqueda` + `buscarNegociosPublicados`; tests en `tests/buscador-pagina.test.ts`; verificado también con `next dev -p 3100` contra `prisma/dev.db` sembrada. — **b-dev (iteración 2)**: por el hallazgo M-1, el eco de la consulta sanea controles y marcas bidi y recorta por puntos de código.
- [x] 12. Agregar el estado sin resultados con sus literales: `No encontramos negocios para "<consulta>".`, "Prueba con otra palabra o elige una categoría:" y los 8 botones de categoría del catálogo enlazados a sus listados (los mismos de la home). Verificable: test de una búsqueda sin coincidencias que muestra los dos textos, los ocho enlaces y sigue respondiendo 200. — **a-ui**: implementado con el mock. — **b-dev**: verificado con la búsqueda real (`?q=veterinario espacial`), en test y con curl (200, ocho enlaces, ninguna tarjeta).
- [x] 13. Agregar el estado de consulta vacía: `h1` "¿Qué estás buscando?", aviso "Escribe qué necesitas y te decimos quién lo hace en Tizayuca.", buscador vacío y las categorías; sin listar ningún negocio. Verificable: `/buscar` sin parámetro, con `?q=`, con `?q=%20%20` y con `?q=%` muestran ese estado, con un solo `h1` y sin tocar la base. — **b-dev**: los 4 casos más emojis y otro alfabeto en `tests/buscador-pagina.test.ts`; "sin tocar la base" queda garantizado por `buscarNegociosPublicados` (test con espía en `tests/busqueda-consultas.test.ts`) y porque la página ni la llama.
- [x] 14. Declarar la metadata de robots de la página de resultados (`noindex`, `follow`). Verificable: test sobre la metadata exportada de la ruta; home, listado y ficha siguen sin `noindex`. — **a-ui**: `export const metadata`. — **b-dev**: test sobre `metadata.robots` y barrido de `src/app` para que ninguna otra página declare `noindex`; confirmado con curl (`<meta name="robots" content="noindex, follow">`).
- [x] 15. Escribir la suite adversarial del buscador (`tests/buscador-adversarial.test.ts`): consulta de miles de caracteres, `q` repetido, `q` como arreglo, `%`/`_`, comillas y `<script>`, emojis y otro alfabeto, y una consulta que coincide con un negocio `en_revision`. Verificable: ninguna produce error; ninguna filtra datos de negocios no publicados; ninguna devuelve el directorio completo. — **b-dev**: 59 casos (22 consultas hostiles × página + consulta, comodines, inyección SQL, fuga de la ficha en revisión con su token/teléfono/dirección, `q` repetido/arreglo/vacío, escapado y recorte).
- [x] 16. Actualizar `tests/layout.test.ts`: `/buscar` entra a la lista blanca de rutas existentes y la revisión de enlaces se extiende al `action` de los formularios, fallando si apunta a una ruta inexistente. Verificable: la suite pasa con el buscador puesto y falla si se cambia el `action` por una ruta inventada. — **b-dev**: `problemasDeEnlaces` revisa formularios; el barrido de literales del código cubre `href` y `action`; se agregó el render de `/buscar` (con y sin consulta) a la revisión y el caso negativo (`action="/buscador"` → señalado). Además se actualizaron los dos scenarios derogados por este change (`tests/directorio-paginas.test.ts` "todo control de la home lleva a algo" y `tests/directorio-adversarial.test.ts` "sin JavaScript de cliente", que ahora exige ausencia de manejadores de eventos y `action`/`method` en los formularios, no ausencia de `<form>`).
- [x] 17. Revisar a 390px, 768px y 1280px la home con buscador, una página de resultados con varios negocios, una sin resultados y una con consulta vacía: sin scroll horizontal, áreas táctiles ≥44px, el botón de WhatsApp sigue siendo lo más prominente de cada tarjeta y una consulta larguísima no desborda el encabezado. Verificable: inspección en los tres anchos sobre el sitio servido; lo que necesite ojos humanos queda anotado para el PR. — **a-ui**: revisión estructural y por código. — **b-dev**: re-verificado con `next dev -p 3100` contra la base real (los cuatro estados responden 200; `min-h-11` en campo y botón, `min-h-16` en las categorías, `break-words` en los encabezados; una consulta de 5 000 caracteres se recorta a 80 + "…" en el `h1` y a 80 en el campo). **Sigue pendiente de ojos humanos**: no hay navegador en este entorno, así que nadie ha visto las tres anchuras de verdad. Anotado para el PR.
- [x] 18. Repasar todos los textos nuevos carácter por carácter contra la spec y contra el español mexicano coloquial del proyecto, y confirmar que ningún dato ficticio nuevo del seed se parece a un negocio real de Tizayuca. Verificable: diff de literales contra el delta de spec y revisión del archivo de seed de demostración. — **a-ui**: literales copiados literal de la spec. — **b-dev**: los tests comparan contra los literales de la spec (no contra el código), así que un cambio de copy rompe la suite. En `prisma/seed-demo.ts` no se agregaron negocios nuevos: solo giros del catálogo y el acento de "Fútbol"; los WhatsApp siguen en `771999xxxx`. Las fixtures nuevas de tests usan `7719993xxx` y `7719998xxx`, nombres inventados con marca ("ficticia", "Inventada") y ninguna dirección real.

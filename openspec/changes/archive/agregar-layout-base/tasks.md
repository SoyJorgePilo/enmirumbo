# Tareas: agregar-layout-base

- [x] 1. Definir los tokens de diseño en el bloque `@theme` de `src/app/globals.css`: paleta neutral (fondo, superficies, texto), los dos tokens verdes de acción (marca #25D366 y variante oscura AA, ver `design.md` §2) con comentario que documente el verde como color de acción principal, y la tipografía como pila de sistema. Verificable: las utilidades generadas por los tokens compilan y el archivo documenta el color de acción.
- [x] 2. Retirar los restos de la plantilla en `globals.css`: media query de dark mode y variables de Geist. Verificable: `grep` sin `prefers-color-scheme` ni `geist` en el archivo y el build pasa.
- [x] 3. Verificar contraste AA (≥4.5:1) de cada combinación texto/fondo definida por los tokens (incluido texto sobre/en verde) con una herramienta de contraste; ajustar valores si alguna falla. Verificable: tabla de combinaciones y ratios anotada en el reporte del change.
- [x] 4. Crear el componente de header (Server Component): wordmark tipográfico "NecesitoUno" + posicionamiento "Tizayuca", enlazado a `/`, con área táctil ≥44px y solo clases de tokens. Verificable: se renderiza en `<header>` sin `"use client"`.
- [x] 5. Crear el componente de footer (Server Component): "NecesitoUno Tizayuca" y espacio previsto para las páginas legales de E6 sin enlaces muertos. Verificable: ningún `<a>` apunta a rutas inexistentes.
- [x] 6. Reescribir `src/app/layout.tsx`: `lang="es-MX"`, metadata base con el título y la descripción literales de la spec, retiro de `next/font`/Geist, y estructura `header` + `main` (flex-1) + `footer` para que el footer quede abajo aun con poco contenido. Verificable: el HTML servido muestra lang, title y meta description correctos.
- [x] 7. Reemplazar `src/app/page.tsx` por la home provisional: `h1` de bienvenida y la frase literal de la spec, sin imports de la plantilla (`next/image`, logos de Vercel/Next). Verificable: la ruta `/` no contiene ningún texto ni asset de la plantilla.
- [x] 8. Revisar responsive: viewport de 390px sin scroll horizontal y adaptación a tablet/escritorio (ancho máximo centrado), con jerarquía de encabezados correcta (un solo `h1`). Verificable: inspección manual a 390px, 768px y 1280px sin overflow horizontal.
- [x] 9. Confirmar que layout, header, footer y home no envían JS de cliente propio: sin `"use client"` en esos archivos y build de producción sin bundles de cliente nuevos atribuibles al layout. Verificable: `grep` + salida de `next build`.

# Tareas: agregar-formulario-registro

- [x] 1. Crear el cliente Prisma de aplicación en `src/lib/` (instancia única reutilizada en desarrollo, adaptador better-sqlite3, `DATABASE_URL`), separado del cliente de pruebas. Verificable: un Server Component puede consultar los catálogos sin abrir una conexión por render. (`src/lib/prisma.ts`, instancia perezosa en `globalThis`; `tests/registro-pagina.test.ts` comprueba que se reutiliza y que `/registro` lee los catálogos de la base)
- [x] 2. Escribir `normalizarWhatsapp` en `src/lib/` según `design.md` §3 (quita separadores, quita prefijo 52/521, devuelve 10 dígitos o `null`). Verificable: tests unitarios con "+52 771 123 4567", "771-123-4567", "7711234567", "771 123 45" y "no tengo". (`src/lib/whatsapp.ts`, `tests/whatsapp.test.ts`)
- [x] 3. Escribir el módulo de validación del registro en `src/lib/` (campos obligatorios, existencia de categoría y colonia en catálogo, cotas de longitud de la tabla de `design.md` §3, colonia "Otra" con texto obligatorio, link de Facebook solo `http(s)`), devolviendo errores por campo con los textos literales de la spec. Verificable: tests unitarios de cada regla, incluido `javascript:` en el link de Facebook. (`src/lib/registro/validacion.ts` + `textos.ts` + `tipos.ts`, `tests/registro-validacion.test.ts`)
- [x] 4. Definir los ejemplos de "¿Qué ofreces?" de las 8 categorías en un módulo de `src/lib/` (los de "Servicios del hogar" y "Clubes y escuelas deportivas" son literales del PRD; los otros 6 se redactan en español mexicano coloquial y se revisan en el PR). Verificable: hay un ejemplo por cada slug de categoría del catálogo y uno genérico de respaldo. (`src/lib/registro/ejemplos.ts`, movido desde el archivo mock de la etapa UI)
- [x] 5. Redactar el bloque de aviso simplificado y consentimiento como componente de servidor, con el texto literal de la spec y sin ningún enlace (E6 pendiente), dejando un comentario que marque dónde va el enlace al aviso integral. Verificable: `grep` sin `<a`/`<Link` en ese componente. (`src/components/registro/aviso-consentimiento.tsx`; el dev lo conectó a `src/lib/registro/textos.ts` y el test comprueba que el HTML no trae ningún `href`)
- [x] 6. Construir la página `/registro` (Server Component): `h1` "Registra tu negocio gratis", los 5 obligatorios y los 6 opcionales con las etiquetas literales de la spec, listas de categorías y colonias leídas de la base más la opción "Otra", opcionales marcados como tales, y el bloque de consentimiento de la tarea 5. Verificable: la página renderiza las 8 categorías y las 21 colonias del seed. (`src/app/registro/page.tsx`; catálogos ya vía Prisma, `force-dynamic` porque en CI no hay base al construir)
- [x] 7. Implementar la Server Action de registro: honeypot, límite por IP, validación con el módulo de la tarea 3, normalización del número, consulta de duplicado, creación del negocio con estado y origen por defecto y `consintioAvisoEn` puesto por el servidor, y `redirect` a la pantalla de gracias. Verificable: un envío válido crea una fila `en_revision`/`organico` y una recarga posterior no crea otra. (`src/app/registro/accion.ts` delgada + `src/lib/registro/procesar.ts` probado en `tests/registro-accion.test.ts`; POST real verificado a mano: 303 a `/registro/gracias`)
- [x] 8. Blindar la acción contra campos no confiables: ignorar cualquier `estado`, `origen`, `publicadoEn`, `tokenGestion` o fecha de consentimiento que venga del cliente. Verificable: test que envía esos campos y confirma que la fila queda en `en_revision`, origen `organico`, sin publicación ni token. (por construcción: `leerEnvioRegistro` solo lee los campos del formulario; test "ignora estado, origen, publicadoEn, tokenGestion...")
- [x] 9. Manejar el duplicado en los dos caminos: consulta previa por número normalizado y captura del error de unicidad de la base, ambos con el mensaje literal de la spec junto al campo de WhatsApp. Verificable: test que registra "7711234567" y luego intenta "+52 771 123 4567". (tests "detecta el duplicado aunque venga con otro formato" y "la unicidad de la base también se traduce al mensaje de duplicado")
- [x] 10. Devolver y pintar los errores por campo conservando lo capturado (menos el checkbox), con el foco en el primer campo con error y los mensajes asociados al input para lectores de pantalla. Verificable: envío vacío muestra los 5 mensajes y un envío con datos + error conserva los valores. (`tests/registro-accion.test.ts` y `tests/registro-pagina.test.ts`; se corrigió `ORDEN_CAMPOS_PARA_FOCO` para que siga el orden real de la pantalla — el consentimiento va al final, no en medio)
- [x] 11. Crear el componente cliente del ejemplo dinámico, acotado al `select` de categoría y al campo "¿Qué ofreces?", que no borra lo escrito al cambiar de categoría. Verificable: sin JS el campo muestra el ejemplo genérico y el formulario sigue enviándose. (dentro de `src/components/registro/formulario-registro.tsx`, ver reports/a-ui.md sobre por qué todo el formulario terminó siendo cliente)
- [x] 12. Crear el botón de envío con estado "Enviando..." deshabilitado durante el envío (componente cliente mínimo). Verificable: dos toques seguidos crean un solo registro. (`src/components/registro/boton-enviar.tsx`)
- [x] 13. Crear la pantalla de gracias con el mensaje literal del PRD §6.1 y un enlace de regreso a la home. Verificable: el texto coincide carácter por carácter con el del PRD. (`src/app/registro/gracias/page.tsx`)
- [x] 14. Implementar el anti-abuso del PRD §8: campo trampa invisible y no enfocable (`tabindex="-1"`, `aria-hidden`, fuera de pantalla, `autocomplete="off"`), límite de 3 envíos por hora por IP con el mensaje literal de la spec, y advertencia en el log cuando las altas del día superan el umbral configurable. Verificable: envío con honeypot lleno no guarda y muestra la pantalla de gracias; el cuarto envío en una hora se rechaza. (`src/lib/registro/limite-ip.ts`, `tests/registro-limite-ip.test.ts` y los tres tests de anti-abuso de `tests/registro-accion.test.ts`; honeypot verificado también con un POST real)
- [x] 15. Confirmar que ningún dato capturado (número, nombre, dirección) aparece en logs ni en mensajes de error mostrados al usuario. Verificable: revisión de cada `console.*`/logger de la ruta y del texto del error genérico. (los 7 `console.*` viven solo en `procesar.ts` y registran eventos, conteos o `resumenDeError` — código de Prisma o nombre del error, nunca su mensaje; test "ningún log de la ruta interpola datos capturados")
- [x] 16. Agregar el enlace "Registra tu negocio gratis" en la home provisional con estilo de acción principal (verde de acción) y área táctil ≥44px. Verificable: la home enlaza a `/registro` y el texto coincide literalmente. (`src/app/page.tsx`; test nuevo en `tests/layout.test.ts`)
- [x] 17. Actualizar la lista blanca de hrefs de `tests/layout.test.ts` (PR #4) para admitir `/registro` y que siga fallando ante enlaces a rutas inexistentes. Verificable: la suite pasa con el enlace nuevo y falla si se agrega un href a una ruta que no existe. (la lista blanca ya no es la constante `"/"`: se deriva de los `page.tsx` de `src/app`, así que un href a una ruta que no existe rompe la suite. También se acotó el test de `"use client"` a los archivos de `layout-base`, que es lo que dice su requirement)
- [x] 18. Escribir los tests de integración del flujo: alta solo con obligatorios, alta con opcionales, colonia "Otra" pendiente de normalizar, categoría/colonia fuera del catálogo, "¿Qué ofreces?" de 250 caracteres y consentimiento sin marcar. Datos ficticios con prefijo `771999` (repo público + LFPDPPP). Verificable: `npm test` en verde. (`tests/registro-accion.test.ts`, 23 casos)
- [x] 19. Revisar la página a 390px, 768px y 1280px: sin scroll horizontal, controles ≥44px, teclado numérico en el campo de WhatsApp y un solo `h1`. Verificable: inspección manual en los tres anchos y revisión del HTML. (revisión del HTML servido: un solo `h1`, `inputmode="numeric"` en WhatsApp, header/footer del layout, controles `py-3`/`min-h-11`, ningún ancho fijo — el único elemento posicionado es el honeypot, desplazado a la izquierda, que en LTR no genera scroll. **Pendiente para el humano del PR**: la comprobación visual en un navegador real en los tres anchos; en este entorno no hay pipeline de capturas)

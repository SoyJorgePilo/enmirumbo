# Tareas: agregar-foto-negocio

- [ ] 1. Agregar `sharp` como dependencia y confirmar que el CI la instala y compila en el runner (`proposal.md`, "Por qué sharp"). Verificable: `npm ci && npm run build && npm test` en limpio; la suite de CI queda en verde con la dependencia puesta.
- [ ] 2. Subir `serverActions.bodySizeLimit` en `next.config.ts` a `6mb` (`design.md` §6). Verificable: un envío del formulario con un archivo de 5 MB llega a nuestra validación (se ve el mensaje de la spec) en vez de fallar con el error genérico de Next.
- [ ] 3. Crear el puerto de almacenamiento `src/lib/fotos/almacen.ts` (guardar/leer/borrar por clave y variante) con el adaptador local a `FOTOS_DIR` (default `.fotos/`, agregado a `.gitignore`), `design.md` §1. Verificable: tests que guardan, leen y borran contra un directorio temporal; borrar una clave inexistente no truena; ninguna ruta escrita puede salirse del directorio configurado (intento con `../`).
- [ ] 4. Crear `src/lib/fotos/clave.ts`: generación de la clave opaca aleatoria y su validación de forma (`design.md` §4). Verificable: dos claves seguidas nunca son iguales; la clave no contiene el id ni el nombre del negocio; cadenas hostiles (`../`, `data:…`, `https://…`, rutas absolutas, cadena vacía) se reconocen como inválidas.
- [ ] 5. Crear `src/lib/fotos/procesar.ts` con la validación por contenido: tope de 5 MB, tope de 40 megapíxeles, solo JPG/PNG/WebP abribles, SVG y archivos disfrazados rechazados (`design.md` §2). Verificable: tests con fixtures generadas en el propio test (JPG válido, PNG válido, WebP válido, HTML con extensión `.jpg`, SVG, PNG de 100 MP, archivo de 6 MB); ninguna fixture binaria se versiona.
- [ ] 6. Completar `procesar.ts` con la generación de las dos variantes (tarjeta 400px ≤60 KB, ficha 1200px ≤250 KB, WebP, rotación aplicada y metadatos descartados). Verificable: tests de dimensiones y peso máximo de las dos variantes sobre una foto de 5 MB; una foto vertical con EXIF de orientación sale derecha.
- [ ] 7. Escribir el test de metadatos: ninguna variante generada conserva EXIF, y en particular ninguna conserva coordenadas GPS. Verificable: se procesa una fixture con EXIF+GPS generada en el test y se inspeccionan los bytes de salida; el test falla si alguien reactiva el copiado de metadatos.
- [ ] 8. Renombrar en `prisma/schema.prisma` la columna `fotoUrl` a `fotoClave` (opcional, `@unique`) y escribir a mano la migración con `ALTER TABLE … RENAME COLUMN`, conservando los `CHECK` de `estado` y `origen` (`design.md` §4). Verificable: base desde cero migra y siembra sin error; `prisma migrate status` sin drift; los `CHECK` siguen en el esquema resultante.
- [ ] 9. Crear el validador de render en `src/lib/fotos/` que recibe lo guardado en `fotoClave` y devuelve la URL interna construida por el servidor o `null` (cierre de M1 de T-004). Verificable: tests con `https://evil.example/x.png`, `data:image/svg+xml,…`, `javascript:alert(1)`, `../../etc/passwd`, ruta absoluta y cadena vacía → todos `null`; una clave válida → la ruta interna esperada.
- [ ] 10. Crear el route handler `src/app/api/foto/[clave]/[variante]/route.ts` (`design.md` §3): 404 si la clave no existe; sirve con caché moderada si el negocio está `publicado`; con sesión válida del panel sirve también los no publicados, con `no-store`; sin sesión, el mismo 404 que si no existiera. Verificable: tests de los cuatro casos (publicado, `en_revision` sin sesión, `en_revision` con sesión, clave inventada), comparando que las dos respuestas 404 son indistinguibles; variante desconocida → 404.
- [ ] 11. Agregar al formulario (`src/components/registro/formulario-registro.tsx`) el campo "Foto de tu negocio (opcional)" con su texto de ayuda literal, el `accept` de imagen, el área táctil ≥44px y la casilla "Dejar mi ficha sin foto"; el formulario pasa a enviarse como multipart. Verificable: el HTML trae los literales de la spec, la etiqueta asociada al campo, y el formulario sigue sin `"use client"` nuevo ni JavaScript asociado a la foto.
- [ ] 12. Agregar los mensajes de error de foto a `src/lib/registro/textos.ts` y su validación de tamaño/tipo declarado a `src/lib/registro/validacion.ts`. Verificable: los tres literales ("Esa foto pesa más de 5 MB. Sube una más ligera.", "No pudimos leer esa foto. Sube una imagen JPG, PNG o WebP.", "No pudimos preparar tu foto. Intenta con otra.") viven en un solo lugar y los tests comparan contra la spec, no contra el código.
- [ ] 13. Enganchar el procesamiento en `src/lib/registro/procesar.ts` como paso 4.5, después del honeypot, el cupo por IP, la validación y la comprobación de duplicado (`design.md` §5). Verificable: tests que confirman que un envío con trampa llena, con IP sin cupo o con número duplicado no procesa imagen ni deja archivos (espía sobre el almacén).
- [ ] 14. Implementar la limpieza de huérfanos: si la escritura en la base no se concreta (error, duplicado, `updateMany` sin filas), se borra la clave recién guardada. Verificable: tests que fuerzan cada fallo y comprueban que el directorio del almacén queda como estaba.
- [ ] 15. Implementar en el reenvío tras rechazo el cambio y el quitado de foto: clave nueva primero, actualización de la fila después, borrado de la clave anterior solo si la actualización afectó una fila. Verificable: tests de los tres casos (foto nueva, casilla marcada, envío que no toca la foto) más el caso de la ficha ya resuelta por el admin, donde la foto anterior queda intacta.
- [ ] 16. Devolver el aviso "Tu foto no se quedó guardada: vuelve a elegirla antes de enviar." cuando un envío con foto se rechaza por otro error. Verificable: test de un envío con foto y WhatsApp inválido → aparece el aviso junto al campo de foto, el resto de los valores vuelve al formulario y no queda ningún archivo guardado.
- [ ] 17. Cambiar `src/components/directorio/marcador-foto.tsx` para que reciba la clave (no una URL), la pase por el validador de la tarea 9 y pinte el marcador cuando devuelva `null`; ajustar `src/lib/directorio.ts` para proyectar la clave. Verificable: tests de render con clave válida, con clave hostil y sin clave; ninguna respuesta HTML contiene un dominio externo.
- [ ] 18. Poner la foto real en la tarjeta (`tarjeta-negocio.tsx`) con `alt` "Foto de <nombre del negocio>", dimensiones declaradas, variante de tarjeta y carga diferida salvo en la primera fila. Verificable: el HTML de un listado pide solo variantes de tarjeta, las de abajo del pliegue van con carga diferida y el espacio de la tarjeta es el mismo con y sin foto.
- [ ] 19. Poner la foto en la ficha (`src/app/negocio/[ficha]/page.tsx`) con la variante de ficha y el mismo `alt`; sin foto, la ficha no muestra hueco ni marco. Verificable: tests de ficha con foto y sin foto.
- [ ] 20. Mostrar la foto en el detalle del panel bajo el rótulo "Foto del negocio", con "Sin foto" cuando no hay. Verificable: tests del detalle con y sin foto, más el caso de que la dirección de esa foto no sirve nada sin sesión.
- [ ] 21. Implementar el borrado definitivo que arrastra los archivos (`src/lib/negocio.ts`), incluido el caso de archivos ya ausentes. Verificable: test que borra un negocio con foto y comprueba que no quedan variantes en el almacén ni la fila en la base, y otro con la foto ya borrada que no truena.
- [ ] 22. Sembrar fotos en `prisma/seed-demo.ts`: al menos un publicado con foto generada al vuelo y al menos uno sin foto, idempotente y sin archivos de imagen versionados. Verificable: dos corridas seguidas dejan una sola foto por negocio sembrado y `git status` limpio.
- [ ] 23. Escribir la suite adversarial de la foto (`tests/foto-adversarial.test.ts`): archivo disfrazado, SVG, bomba de píxeles, 6 MB, varios archivos en el mismo campo, nombre de archivo con `../` y con caracteres raros, clave inventada en la ruta que sirve fotos, foto de ficha en revisión pedida sin sesión, y clave hostil guardada a mano llegando al render. Verificable: ninguna produce error 500, ninguna filtra datos de negocios no publicados y ninguna sirve un archivo fuera del almacén.
- [ ] 24. Documentar en el `README.md` la variable `FOTOS_DIR`, dónde caen las fotos en desarrollo y que el almacenamiento de producción depende de E0-3 (ADR-006). Verificable: quien clona el repo puede registrar un negocio con foto, aprobarlo y verla en el directorio siguiendo solo el README.
- [ ] 25. Revisar a 390px, 768px y 1280px el formulario con el campo de foto, un listado con fotos y sin ellas, una ficha con foto y el detalle del panel: sin scroll horizontal, áreas táctiles ≥44px, la foto no le roba protagonismo al botón de WhatsApp y nada salta al cargar. Verificable: inspección en los tres anchos sobre el sitio servido; lo que necesite ojos humanos queda anotado para el PR.
- [ ] 26. Repasar carácter por carácter los textos nuevos contra la spec y contra el español mexicano coloquial del proyecto, y confirmar que ninguna foto ni dato sembrado se parece a algo real. Verificable: diff de literales contra el delta de spec y revisión del seed de demostración.

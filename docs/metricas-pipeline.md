# Métricas del pipeline multiagente

> El pipeline se mide por retrabajo, no por volumen (ver `docs/proceso.md` §Medición). Una fila por corrida de `/implementar` o `/rapido`. "1a pasada" = veredicto del validador sin necesidad de regresar a etapas previas. "Post-merge" = commits de corrección sobre esa feature en las 2 semanas siguientes (se rellena después, normalmente en el `/checkpoint`).
>
> Las filas solo son comparables entre sí mientras el reparto de modelos por agente no cambie (ADR-008). Si se toca, se anota aquí la fecha del cambio y las corridas anteriores no se promedian con las posteriores.

| Fecha | Change/fix | Ruta | Iter. C (dev↔seg) | Hallazgos validador | 1a pasada | PR | Post-merge |
|---|---|---|---|---|---|---|---|
| 2026-09-03 | agregar-modelo-datos | completa (sin etapa A: change sin UI) | 0 | 0 bloqueantes (1 editorial: línea redundante en `.gitignore`) | sí | [#2](https://github.com/SoyJorgePilo/necesitouno/pull/2) | 1 corrección diferida y saldada: el M2 de c-seguridad (documentar `DATABASE_URL` en un `.env.example` versionado) se cerró post-merge en el PR #4 (`8ad7093`). Nada más al 2026-09-04 |
| 2026-09-03 | agregar-layout-base | completa | 0 | 0 bloqueantes (M-1 operativo de c-seguridad atendido con staging explícito) | sí | [#3](https://github.com/SoyJorgePilo/necesitouno/pull/3) | 2 correcciones: la deuda declarada en b-dev (portar a Vitest los scenarios automatizables de la spec) se saldó en el PR #4 (`8ad7093`) y `7914739` limpió un encabezado huérfano en la spec consolidada. Nada más al 2026-09-04 |
| 2026-09-03 | deuda-post-merge | corta | — | 1 alto corregido (`.env.example` ignorado por `.gitignore`) | no | [#4](https://github.com/SoyJorgePilo/necesitouno/pull/4) | ninguna conocida al 2026-09-04 |
| 2026-09-03 | agregar-formulario-registro | completa | 1 (1 alto + 6 medios, corregidos y re-verificados en la iteración) | 0 bloqueantes (3 notas informativas: no-op en un test, convención de teléfonos ficticios, fusión de scenarios al archivar) | sí | [#5](https://github.com/SoyJorgePilo/necesitouno/pull/5) | ninguna conocida al 2026-09-04 |
| 2026-09-03 | agregar-directorio-publico | completa | 1 (0 críticos/altos; 2 medios corregidos —`tel:` sin normalizar y guarda del seed— y re-verificados) | 0 bloqueantes (1 decisión de producto tomada: el fijo no marcable como texto; 5 notas: format-detection, convención de fijos ficticios —reincidente—, marcador de foto en la ficha, salto h1→h3 en el listado, conteo real 14 req/54 sc) | sí | [#6](https://github.com/SoyJorgePilo/necesitouno/pull/6) | ninguna conocida al 2026-09-04 |
| 2026-09-03 | agregar-panel-admin | completa | 1 (1 alto + 4 medios corregidos en iteración; veredicto final limpio con 5 bajos documentados) | 0 bloqueantes (1 corrección editorial aplicada por el validador: `tasks.md` #22 seguía describiendo la constancia de consentimiento como se decidió ANTES de la enmienda del `design.md` §6; 3 notas: prop `motivoPrevio` sin usar, "Revisar" `aria-hidden` en la tarjeta de la cola, revisión visual a 390px pendiente del humano) | sí | [#8](https://github.com/SoyJorgePilo/necesitouno/pull/8) | ninguna conocida al 2026-09-04 |
| 2026-09-03 | agregar-buscador | completa | 2 (0 críticos/altos; 4 medios + 2 bajos en la 1a pasada — M-1/M-2/M-3/B-1 corregidos y re-verificados; B-3 y la muletilla "tizayuca" en la iteración final; M-4 y B-2 aceptados como deuda) | 0 bloqueantes (1 desviación de spec informativa: la lista de muletillas no está en el requirement "se exigen todas" y hay que redactarla al consolidar; 3 deudas trasladadas al PR) | sí | [#7](https://github.com/SoyJorgePilo/necesitouno/pull/7) | ninguna conocida al 2026-09-04 |
| 2026-09-04 | agregar-paginas-legales | completa | 1 (1 alto + 3 medios; ALTO-1 y MEDIO-1/2 corregidos con enmienda de spec aprobada y re-verificados contra el texto servido; MEDIO-3 cerrado en la propia iteración; veredicto final limpio con 1 bajo residual) | 0 bloqueantes (contenido legal verificado byte por byte: los 3 literales aprobados —aviso, términos y aviso simplificado— coinciden con la spec enmendada; 1 bajo residual trasladado al PR: la purga de rechazados a los 90 días sigue sin ejecutor, E0-3; 5 pendientes humanos listados en el PR) | sí | [#9](https://github.com/SoyJorgePilo/necesitouno/pull/9) | 1 enmienda al literal del aviso en el PR #12 (`baa6f52`), necesaria porque la foto de T-008 lo volvió falso — defecto de integración detectado en la corrida de la foto, no de esta. Nada más al 2026-09-04 |
| 2026-09-04 | agregar-seo-local | completa (sin etapa A: reutilización de UI justificada y verificada por seguridad) | 1 (0 críticos/altos; 4 medios — M1 og:image a localhost en el build, M2 teléfono en texto libre hacia meta/JSON-LD y M4 6→3 consultas corregidos y re-verificados; M3 `fotoUrl` sin lista blanca delegado a T-008 por coordinación de merge; 5 bajos documentados) | 0 bloqueantes (1 corrección editorial aplicada por el validador: `Object.freeze` + tipos `readonly` en los catálogos memoizados, R3; 1 recomendación de la etapa C **declinada con medición propia**: NFKC + `\p{Cf}` no cierra los dígitos arábigo-índicos como afirmaba el reporte y reescribe copy legítimo —"½"→"1⁄2", "Niños²"→"Niños2"—; 5 notas al PR) | sí | [#10](https://github.com/SoyJorgePilo/necesitouno/pull/10) | el M3 delegado a T-008 (`fotoUrl` sin lista blanca hacia `og:image`/JSON-LD) se cerró ya mergeado este PR, dentro del PR #11 (cierre verificado en `archive/agregar-foto-negocio/reports/d-validacion.md` §9). Nada más al 2026-09-04 |
| 2026-09-04 | agregar-foto-negocio | completa | 3 (1 alto A-1 —DoS de descompresión con imagen válida de 39 MP— cerrado con semáforo de 2 decodificaciones sin cola; medios M-1 a M-6 corregidos en las tres iteraciones, incluido el que introdujo la propia corrección —el turno retenía 1.8 s y bloqueaba el formulario a 1 req/s, ahora 13 ms—; veredicto final limpio con 3 bajos) | 1 bloqueante NO del código: el aviso de privacidad de T-007 dice "el formulario todavía no pide fotos" y este change lo vuelve falso; el literal está fijado por la spec `paginas-legales`, así que necesita enmienda con aprobación humana (PR abierto en borrador). Sin scope creep; 1 imprecisión menor en `tasks.md` #12 (los tests comparan contra las constantes, no contra la spec: la comparación literal la hizo el validador). Cierra además el M3 que T-009 le delegó: `og:image` y JSON-LD pasan a construir la URL desde la clave interna, lista blanca por construcción | no | [#11](https://github.com/SoyJorgePilo/necesitouno/pull/11) | 1 enmienda legal: el aviso de privacidad quedó falso al entrar la foto a main ("el formulario todavía no pide fotos") y se corrigió el mismo día en el PR #12 (`baa6f52`) — es el bloqueante que este validador dejó declarado al abrir el PR en borrador. Nada más al 2026-09-04 |
| 2026-09-04 | agregar-despublicar-y-borrado-arco | completa | 1 (0 críticos/altos; 2 medios —M1 borrado concurrente a mitad de una aprobación que reventaba la Server Action con P2025, M2 el renglón de pendientes legales se llevaba de más los derechos ARCO que el panel sigue sin atender— y 3 bajos corregidos y re-verificados; veredicto final limpio) | 0 bloqueantes (1 desviación de spec deliberada y aceptada: el delta de `paginas-legales` dice "se retira" el pendiente del flujo ARCO y la implementación lo **acota** a acceso y rectificación — hay que redactarlo así al consolidar; 1 nota: el detalle repinta los giros previos si el admin los desmarca todos y falla otra validación; 2 deudas al backlog: el recorte silencioso del motivo en `rechazarRegistro` y bitácora+2FA de las acciones destructivas). Integración verificada por el validador: el punto de la foto en el hard delete queda ACTIVO tras fusionar T-008 (`borrarNegocio` delega en `borrarNegocioDefinitivamente`, que pasa a `deleteMany`), y la suite de "la despublicada desaparece de todo" cubre las superficies nuevas de T-009 (giro, giro+colonia y sitemap) | sí | [#13](https://github.com/SoyJorgePilo/necesitouno/pull/13) | ninguna conocida al 2026-09-04 |
| 2026-09-04 | agregar-analitica-cookieless | completa (sin etapa A: el change no dibuja pantallas nuevas; solo suma atributos de marcado a botones existentes, justificado en la propuesta) | 2 (1 alto A-1 —el referente del panel entregaba `/admin/registros/<id>` al proveedor— y 4 medios en la 1a pasada; la propia corrección introdujo el A-2, que rompía el panel sin JavaScript con `Origin: null` → 500, cerrado con `strict-origin`; veredicto final limpio con 1 observación de redacción) | 2 altos corregidos en 3 iteraciones. 0 bloqueantes del validador. 1 corrección editorial aplicada (O-2: el scenario del referente describía `no-referrer`, el valor que rompía el panel). Integración mayor verificada por mí: el grupo `(publico)` convive con el `[destino]` de T-009 y la foto de T-008 sin cambiar **ninguna** URL (23 rutas idénticas a main, única alta `/admin/[...resto]`); `CAMPOS_FICHA` pisaba el slug de la categoría y mandaba `categoria=otra` en los tres eventos de la ficha —corregido—; la afirmación "la 404 no se mide" quedó imprecisa tras T-009 y se corrigió en el diseño. T-015 se mergeó a mitad de la corrida y sus 3 pantallas nuevas heredaron exclusión y política de referente sin tocar nada | no | [#14](https://github.com/SoyJorgePilo/necesitouno/pull/14) | ninguna conocida al 2026-09-04 |
| 2026-09-04 | versionar-aviso-privacidad | completa (sin etapa A: la superficie nueva son tres líneas de texto y un `input type="hidden"` dentro de componentes que ya existían; sin pantalla, estado ni interacción nueva — justificación verificada por seguridad y por mí) | 1 (0 críticos/altos; 4 medios corregidos y re-verificados — M-1 la marca de borrador quedaba fuera de la huella y se podía retirar del aviso con la suite en verde, M-2 el guardián de fuente única tenía el `1` escrito a mano y habría dejado de vigilar justo al estrenar la `2`, M-3 la reaceptación se decidía con `!==` y tras un rollback anotaba como "más nueva" una versión más vieja, M-4 una constancia sin versión estrenaba reaceptación en el primer reenvío, convirtiendo a cualquiera que conociera el número en autor de evidencia sobre la ficha de otro; 3 bajos aceptados con razón escrita, BAJO-5 con test de guardia) | 0 bloqueantes (2 correcciones aplicadas por el validador: el comentario de `schema.prisma` seguía describiendo la regla de reaceptación **anterior** a M-3/M-4 —la que produce evidencia falsa—, y un literal de prueba que la fusión con T-015 dejaba fuera de tipo, con `npm test` en verde y `npm run build` en rojo; 1 nota informativa: el comentario de la lista blanca de `directorio.ts` no enumera las columnas nuevas). **La fusión con `main` fue la prueba de fuego del diseño**: git resolvió `textos.ts` sin conflicto —las enmiendas de #12 y de este change tocan hunks distintos— y el guardián huella↔versión saltó igual, exigiendo decisión de versión sobre contenido legal que habría cambiado en silencio. Resuelto re-anclando la huella de la `1` según la recomendación escrita del dev: la `1` la estrena este change y todavía no ampara ninguna constancia. Sin drift de migraciones | sí | [#15](https://github.com/SoyJorgePilo/necesitouno/pull/15) | ninguna conocida al 2026-09-04 |
| 2026-09-04 | agregar-boton-reportar | completa | 3 (2 de seguridad + 1 del validador) | 2 altos de carrera + medios corregidos en 3 iteraciones; en la 1a pasada del validador, 1 medio bloqueante (el aviso "Reporte atendido." vivía dentro de la sección de pendientes, así que atender el ÚLTIMO reporte de un negocio no confirmaba nada) + 2 correcciones editoriales (404 uniforme ante bounds no-texto, dependencia del id-cuid escrita en el código) | no | [#16](https://github.com/SoyJorgePilo/necesitouno/pull/16) | ninguna conocida al 2026-09-04 (mergeado hoy; ventana de 2 semanas abierta) |
| 2026-09-04 | fix visual del fundador (contornos+iconos+header) | corta | — (ruta corta: sin etapa C) | 1 iteración del validador. 1 medio bloqueante en la 1a pasada: el ⚽ del bloque de deporte estaba DENTRO del `h2`, sin `aria-hidden`, contra un requirement que exige ese título "literalmente" y sin enmienda que lo cubriera — y para que pasara se había relajado una aserción existente (`toContain` → `endsWith`), así que los tres gates salían verdes con el defecto adentro. Corregido con la opción que pedí: emoji a un `<span aria-hidden>` hermano del `h2`, aserción original restaurada y enmienda escrita en la spec. Elegibilidad de ruta corta arbitrada y confirmada: toca formulario público y panel admin, pero solo el color del contorno; cero lógica, validación, datos o flujo. Ratios recalculados por mí (4.833:1 y 4.397:1) y verificados de punta a punta contra el CSS del build y el HTML de un servidor de producción, no contra el código fuente. 2 bajos al PR (borde del botón secundario, PRD §203 sobre el logo) | no | [#17](https://github.com/SoyJorgePilo/necesitouno/pull/17) | |
| 2026-09-04 | lote visual 2 del fundador (colapso responsivo + horario + fijo validado) | corta (con etapa B: dev) | — (ruta corta: sin etapa C; el dev sí participó) | 1 iteración del validador; los arreglos los aplicó el ORQUESTADOR directamente, no el dev (el transcript del dev ya no existía y eran cambios de una línea). 2 hallazgos en la 1a pasada, los dos míos y ninguno detectado por los gates, que salían verdes con el defecto adentro: **V-1 medio bloqueante** — el `<a>` de categoría no llenaba su celda, así que a 360/390/412px los botones de una misma fila quedaban de alturas distintas (102 vs 82px, escalón muerto bajo el corto), justo contra la enmienda recién aprobada que exige lo contrario, y sin ningún test que lo cubriera; **V-2 bajo** — `pareceTelefono` usaba `\s`, que en JS abarca salto de línea, tab, NBSP y separadores Unicode, mientras la spec dice "espacio, guion, paréntesis y `+`" (sin impacto de seguridad: verifiqué que `construirEnlaceTelefono` solo mete dígitos al `tel:` y el pintado escapa). Corregidos con `h-full` + caso nuevo en el guardián y con la regex a `/^[0-9+() -]+$/` + 3 casos nuevos; re-verificados por mí. **Fe de erratas registrada en el PR**: el "desbordamiento a 390px" que motivó el lote era artefacto de captura —`--window-size=390,844` deja `innerWidth` en 500 en macOS y la imagen se recorta—, reproducido por mí en las dos modalidades; con el viewport emulado `scrollWidth = clientWidth = 390` en las cinco pantallas públicas. Los defectos de colapso sí eran reales. Medí yo por CDP (`Emulation.setDeviceMetricsOverride`, servidor de producción) alturas por fila a 320/360/390/412/768 —todas iguales tras el arreglo— y el colapso 1→2→3. Guardián nuevo sometido a mutación: revertir la retícula a `grid-cols-2` tira 4 tests y quitar `h-full` tira exactamente el caso de alturas. Barrido adversarial propio del fijo: 22 casos + 11 espacios Unicode, todos como manda la spec. 1 propuesta fuera de alcance del dev trasladada al PR como candidata a ticket (enriquecer la ficha mínima, hoy con hueco de sticky footer) | no | [#18](https://github.com/SoyJorgePilo/necesitouno/pull/18) | ninguna conocida al 2026-09-04 |
| 2026-09-04 | preparar-deploy-produccion | completa (sin etapa A: el change no dibuja ninguna pantalla nueva —su superficie visible son dos literales del panel y los avisos del log—, justificado en la propuesta y verificado por mí) | 3 iteraciones dev↔seguridad + 1 consulta al fundador (R4). En total **5 altos y 11 medios en 4 iteraciones**, todos cerrados: A1 el `?host=` que disfrazaba de local una base remota y se llevaba por delante la guarda del seed, A2 el driver `pg` que no cifra salvo que se lo pidan (todo el directorio en claro hacia Supabase), A3 el tope de reportes que dejó de ser atómico al cambiar de dialecto, A4 el límite de acceso al panel contado en memoria de cada instancia serverless, A5 el adaptador de fotos que faltaba —con el borrado ARCO respondiendo "borrado" sin borrar—; y R4, que el fundador resolvió eligiendo que el borrado se niegue a mentir. 122 pruebas adversariales, 7 de ellas nacidas en rojo | no (0 bloqueantes y sin regresar a etapas previas; 1 medio encontrado por mí y corregido aquí: el scenario de la purga en el delta de `modelo-datos` seguía describiendo el orden de borrado ANTERIOR a la decisión del fundador, o sea la única contradicción viva del contrato, que se habría consolidado tal cual a `openspec/specs/`; + 3 correcciones editoriales —comentario invertido en `purga/rechazados.ts`, la línea de SQLite en `CLAUDE.md` y un byte nulo crudo en el reporte de seguridad que volvía el archivo binario para `grep`— y 4 bajos al PR). Re-verificado por mi cuenta, no por reporte: migración desde cero con `INSERT` crudos contra los 4 CHECK, 24 cadenas de conexión hostiles, los 4 avisos de arranque en un `next start` real sin filtrar la contraseña, la purga borrando solo el de 100 días e idempotente, el barrido respondiendo 500 al detenerse, las rutas de tareas en 404 con secreto equivocado y truncado, y los dos guardianes probados por mutación (variable sin documentar y ruta que lee la base al construir: los dos se pusieron rojos) | no | [#19](https://github.com/SoyJorgePilo/necesitouno/pull/19) (el push exigió que el humano concediera el scope `workflow` — el change toca `.github/workflows/ci.yml`) | |
| 2026-09-04 | deuda documental de T-013 (erratas del runbook + comentarios que mentían sobre el flujo de fotos) | corta | — (ruta corta: sin etapa C) | 0 bloqueantes y 0 hallazgos. Elegibilidad de ruta corta re-arbitrada línea por línea: de las 33 líneas de código tocadas, 32 están dentro de bloques `/** */` y la 33a es el literal de un `console.error` del servidor; cero cambios de comportamiento, y ningún texto de los tocados está fijado por `openspec/specs/` (verificado por grep). Lo que verifiqué NO es la prosa nueva sino los hechos que afirma, contra el código: `cuposLimpiados` existe en la respuesta de la purga y su limpieza va en un `try` aparte que no tumba los 90 días (`purga/rechazados.ts:167-178`), el techo son 5 000 filas (`MAX_FILAS_DE_CUPOS`), el alta con foto se rechaza ENTERA antes de crear la ficha (`registro/procesar.ts:288-297`, mensaje `errorProcesamiento` = "No pudimos preparar tu foto. Intenta con otra."), la reaceptación se decide con "posterior" y no con "distinta" (`versionAvisoEsPosterior`, MEDIO-3/4 de T-012) y sus dos columnas se escriben juntas en el mismo `updateMany` —de ahí que el segundo literal de `detalle-registro.tsx` sea inalcanzable—, E3-7 existe en el backlog con ese alcance exacto, la §10 vuelve a numerar 1-9 sin el `8.` duplicado y bajo "Cuatro cosas" hay cuatro. El cambio de literal del log no rompe su prueba (`fotos-supabase.test.ts:122` exige "disco local" y "borrado ARCO": los dos siguen ahí, y la llave sigue sin aparecer) | sí | [#20](https://github.com/SoyJorgePilo/enmirumbo/pull/20) (primer PR con el repo ya renombrado: los enlaces de las filas anteriores siguen vivos por la redirección de GitHub) | |
| 2026-09-04 | franja blanca bajo el pie en Android (`min-h-full` → `min-h-dvh`) | corta | — (ruta corta: sin etapa C) | 0 bloqueantes y 0 hallazgos. **Primer defecto reportado por un usuario real, no por el fundador ni por un agente.** Elegibilidad re-arbitrada: el diff es UN archivo y UNA clase de Tailwind en el `<body>` del layout raíz, más el comentario que documenta el porqué; no toca formulario público, panel, enlaces de gestión ni datos personales, y no contradice `openspec/specs/layout-base` —el requirement pide "un footer al final de la página" y `dvh` lo cumple mejor que `100%`—. Verifiqué que ninguna prueba fijaba `min-h-full` (los `min-h-*` que hay en `tests/` son los `min-h-11` de área táctil, intactos). No me quedé en el diff: comprobé que la clase EXISTE de verdad en el CSS construido (`min-height:100dvh` en el chunk de Tailwind 4) y levanté un `next start` real para leer el HTML servido —`<body class="flex min-h-dvh …">` y el landmark `<footer>` en su sitio—, porque una clase mal escrita en Tailwind no rompe ni el lint ni el build: desaparece en silencio. El comentario JSX agregado dentro de `<html>` no deja nodo en el marcado servido (`{/* */}` no compila a hijo). Gates: lint y build en verde; `npm test` 2 669/2 673, con los 2 casos de carrera real de `reportes-seguridad-adversarial` en rojo por la base local —`prisma dev` corta a 10 conexiones y comparte una sesión, justo lo que `docs/despliegue.md` §2 punto 4 advierte—: reproducidos idénticos en el árbol base con el cambio guardado en stash, y verdes en el CI de main sobre `postgres:17` (corrida 33930097929, mismo commit). Nota de deuda para el checkpoint: ese mismo párrafo del runbook afirma que "las pruebas de carrera real se saltan en local", y no se saltan | sí | [#21](https://github.com/SoyJorgePilo/enmirumbo/pull/21) | |
| 2026-09-04 | deuda TLS del despliegue real: producción sale de `sslmode=no-verify` a `verify-full` con la raíz de Supabase empaquetada | corta | — (ruta corta: sin etapa C) | 0 bloqueantes y 0 hallazgos. Elegibilidad re-arbitrada: `src/` no tiene ni una línea de diff (verificado con `git diff --name-only -- src/`, 0 archivos) — el guard `motivoDeConexionInsegura` queda intacto y el cambio vive en configuración, un archivo de datos y prosa; no toca formulario público, panel, enlaces de gestión ni datos personales. **Lo que no acepté por reporte fue el certificado**: además del `openssl x509` (autofirmado, CN `Supabase Root 2021 CA`, `CA:TRUE`, vigente a 2031, sin `PRIVATE KEY`), lo descargué del origen de Supabase (`supabase-downloads.s3-ap-southeast-1.amazonaws.com/prod/ssl/prod-ca-2021.crt`, HTTP 200, `application/x-x509-ca-cert`) y comparé huellas SHA-256: idénticas (`80:70:25:AD:…:CA:FA`), o sea el archivo del repo ES la raíz pública de Supabase y no un autofirmado cualquiera con el nombre puesto. El `outputFileTracingIncludes` lo verifiqué sobre el build real, no sobre la config: 30 de los 32 `.nft.json` traen el certificado —los 2 que no son `next-server` y `next-minimal-server`, que no son rutas—, y resolví a mano las rutas relativas de la portada (`../../../../certs/…`), de `/negocio/[ficha]` y de `/admin/cola` hasta el archivo existente en disco, que es justo el caso que la clave `/*` de los ejemplos habría dejado fuera. Ningún secreto entra: `git add -An` lista 6 archivos y `.env` está ignorado (`.gitignore:51`). `pg-connection-string` no es dependencia nueva: `src/lib/base-datos/conexion.ts:23` ya la importa. Gates: lint y build en verde; `npm test` 2 680/2 684 con base propia (`prisma dev --name tlsval`, puerto 51242), los 11 tests nuevos de TLS en verde y en rojo SOLO los `[A1]`/`[A2]` preexistentes de carreras PGlite | sí | [#22](https://github.com/SoyJorgePilo/enmirumbo/pull/22) | |
| 2026-09-04 | agregar-listado-gestion-panel | completa | 1 (etapa C limpia a la primera: 0 críticos, 0 altos, 0 medios; 3 observaciones bajas y 82 pruebas adversariales nuevas, ninguna nacida en rojo) | 0 bloqueantes. 6 observaciones bajas, todas al PR. 1 corrección aplicada por mí: la cabecera de `listado-parametros.ts` seguía diciendo "implementación candidata (etapa UI)… no asuman que esto ya está probado", falso desde que la etapa B escribió las suites (solo comentario). Lo que verifiqué por mi cuenta y no por reporte: las 2 rojas heredadas (`[A1]`/`[A2]` de reportes) reproducidas con el change ENTERO en un `stash -u` sobre el árbol base, o sea sin una línea de T-018; la prueba de volumen leída por dentro —el cliente espía anota cuántas filas devolvió la base, así que un `slice` en memoria la pondría roja—; el formato de fecha comprobado en Node contra el ejemplo literal del requirement; y la pantalla contra un `next start` de producción con cookie firmada a mano: 307 sin datos sin sesión, ningún `<form>`/`<button>`/`wa.me`/`tel:`/`771999…` con sesión, `noindex` puesto, sin medición, `?estado=xyz&pagina=-3` idéntica a "Todos" pág. 1, `?pagina=99` con la salida de regreso, y un XSS por querystring que sale escapado. Matiz al reporte C: su "byte a byte idéntica" vale a nivel de render; contra el servidor real la carga RSC sí incluye la URL de la propia petición (escapada, sin vector). Desviación de spec trasladada al humano: la página fuera de rango no pinta texto de vacío —el scenario pide justo eso, la prosa del requirement pide un texto— y hay que redactarlo al consolidar. Tarea 13 (revisión visual a 390/768/1280px y contraste AA) queda abierta a propósito, como en T-005 y T-015 | sí | [#23](https://github.com/SoyJorgePilo/enmirumbo/pull/23) | |
| 2026-09-04 | agregar-enlace-de-gestion | completa | 2 (1 ALTO + 3 medios en la 1a pasada, todos corregidos y re-verificados por comportamiento; el ALTO era la 4a fuga del token —el tracker de la analítica manda el `pathname`, y ahí el pathname ES el secreto—, cerrado moviendo la ruta al grupo `(gestion)`, sin lista de rutas; 1 medio queda como riesgo asumido documentado —token en el log de acceso de la plataforma, `docs/despliegue.md` §8.1— y 1 hallazgo propio del dev al verificar el arreglo: `no-referrer` mandaba `Origin: null` y el envío SIN JavaScript respondía 500, roto un requirement aprobado; se cambió a `strict-origin` en el layout del grupo) | 0 bloqueantes. **Lo que no acepté por reporte fue el comportamiento**: ejercité las funciones de producción contra la base t014 y comprobé que la huella guardada es exactamente `SHA-256(token)` y el token en claro no aparece en ninguna columna, que aprobar dos veces no cambia la huella, que un envío con `estado`, `origen`, `publicadoEn`, `consintioAvisoEn`, la huella y un `negocioId` ajeno metidos a mano deja la fila del negocio byte por byte igual, que las columnas de la edición son las once editables más su ciclo de vida y ninguna prohibida, que un negocio con edición pendiente ocupa UN renglón, que aplicar recalcula el texto del buscador y deja intactos estado, origen, giros, fechas, consentimiento y enlace, que la segunda aplicación responde "ya la habías resuelto", que regenerar mata el enlace viejo y que borrar el negocio se lleva sus ediciones; y por catálogo de PostgreSQL, que el índice único parcial `WHERE estado = 'pendiente'`, el CHECK de los tres estados y las tres claves foráneas (CASCADE/RESTRICT/SET NULL) existen de verdad, y que de `tokenGestion` en claro no queda columna. Revisé además los guardianes enmendados uno por uno buscando aflojamiento: ninguno lo está (el de `admin-adversarial` quedó más estricto). 4 notas para el `/checkpoint`: ajustar la prosa del requirement del `Referer` a "no se manda la RUTA" (aprobado por el orquestador con delegación del fundador), sumar los 7 literales nuevos al change del renombre, subir al backlog que `EdicionPendiente` crece sin poda guardando datos personales (retención sin plazo, LFPDPPP), y la tarea 32 queda en `[~]`: la inspección visual del panel a 390px/1280px es del humano, como la propia tarea previó (la parte estática la revisé). Desliz de proceso del dev (`git stash`/`pop`, §8 de b-dev) evaluado y NO sancionado: lo declaró, no hubo commits y el árbol quedó idéntico. Gates post-merge de `main`: lint, `tsc` y build limpios; `npm test` **104 archivos, 2 915 pasando, 2 saltados, 0 en rojo** —ni los `[A1]`/`[A2]` intermitentes de PGlite— | sí | [#24](https://github.com/SoyJorgePilo/enmirumbo/pull/24) | |
| 2026-09-04 | renombrar-sitio-enmirumbo | completa (sin etapa A: el change no dibuja pantallas, cambia literales — justificado en la propuesta y verificado por mí) | 1 vuelta (etapa C: PASA con 0 críticos y 0 altos; 3 medios —dos pruebas adversariales de T-012 que el rebrand dejó mudas al estrenar la versión `2` del aviso y el mapa scenario→prueba que omitía justamente ese archivo— cerrados por el dev en una segunda vuelta, dos de ellos verificados por mutación) | 0 bloqueantes. **La etapa D fue casi toda integración**: la rama nació antes de que T-018 (PR #23) y T-014 (PR #24) mergearan, y los dos traían literales con la marca vieja. Fusioné `main` dos veces (se movió a media corrida) y el guardián de marca del propio change cazó **6 literales de producto** (los cuatro mensajes nuevos del panel de T-014, el de "Perdí mi enlace") más 9 dominios ficticios, cada uno con archivo:línea; las 5 aserciones que los fijaban se actualizaron junto con el literal, ninguna borrada. 1 defecto de integración más lo cazó un gate y no el barrido: la guardia de migraciones de `tests/aviso-version.test.ts` quedó desfasada al entrar la migración de T-014 — corregida agregando el renglón, no aflojando la aserción. Verifiqué por mi cuenta la huella de la versión `2` (recalculada, 54 piezas, idéntica a la anclada), que la de la `1` no se re-ancló, `PLACEHOLDERS_LEGALES` = 5 en tiempo de ejecución, que ninguna ruta cambió y que la historia del repo no se tocó. 3 observaciones informativas al PR, una de ellas deuda ajena: el checkpoint de T-014 archivó el change **sin consolidar sus deltas**, así que la consolidación de `openspec/specs/` tiene que aplicar T-014 y encima la regla de marca, nunca T-019 sobre el texto viejo | sí | [#25](https://github.com/SoyJorgePilo/enmirumbo/pull/25) | |
| 2026-09-04 | agregar-verificacion-sms-tras-bandera | completa | 2 vueltas + fix final (1a vuelta: 1 ALTO **[C-1]** —cambiar el WhatsApp desde el enlace de gestión conservaba la marca, o sea el panel afirmando propiedad de un número ajeno con el literal aprobado— y 1 MEDIO **[C-2]** —rebobinar la cookie de paso revivía los 5 intentos y los 2 reenvíos, y sin `REGISTRO_ENCABEZADO_IP` declarado eso consumía el tope diario global entero desde un solo registro—; los dos cerrados y re-verificados con ataques más fuertes que los originales, el guardián nuevo probado por mutación. La corrección de [C-2] introdujo **[C-3]** MEDIO —el clic dentro del cooldown gastaba un reenvío sin mandar ningún SMS y al tercero echaba al dueño de la pantalla—, corregido en la iteración 3 invirtiendo el orden de las dos cotas, con las aserciones de la auditoría en verde. Total: 1 alto y 2 medios corregidos; 6 observaciones bajas declaradas) | 0 bloqueantes. 3 bajos, todos al PR. **El foco fue el test de oro**, y no lo di por bueno con las suites del change: monté los componentes de `origin/main` junto a los de la rama en el mismo proceso y comparé el HTML renderizado — detalle del panel y renglón de la cola **idénticos byte a byte**; `/registro/gracias` **NO** ([V-1]): gana un `<div>` contenedor y el `max-w-md` se muda del `h1` a ese `div`, render idéntico y cero texto/campo/enlace/script nuevo, así que el scenario aprobado se cumple y la promesa coloquial "byte a byte" no al pie de la letra. Segundo hallazgo del mismo barrido: **[V-2]** `/registro/gracias` pasa de prerenderizada a dinámica (`ƒ`) porque ahora lee `searchParams` — inherente al diseño aprobado, único cambio de comportamiento observable en producción con la bandera apagada. **[V-3]** residuo funcional: un reenvío bloqueado por el cupo por IP o por el tope diario sí gasta reenvío. Los cupos tampoco los acepté por reporte: mutilé el código real (2→99 reenvíos, 5→99 intentos, 3→300 códigos/IP) y **18 pruebas se pusieron rojas** en las tres suites del canal; revertido y comprobado. Desviación de alcance aceptada y declarada: la corrección de [C-1] toca `src/lib/gestion/` (T-014), que la propuesta ponía fuera del change — sin ella el panel afirma una verificación que no ocurrió. Migración verificada: `ADD COLUMN` nulable y sin default, `migrate status` sin drift, filas viejas con la columna nula. **Trae migración: tras el merge hay que correr `migrate deploy` en producción** | sí | [#27](https://github.com/SoyJorgePilo/enmirumbo/pull/27) | |

## Qué mirar cada ~5 corridas

- Si el validador rechaza seguido por cosas que el CI ya habría atrapado → recortar su checklist mecánico (0 casos en las primeras 10 corridas — ADR-009).
- ¿Los `a-ui.md` traen el contrato de reemplazo completo (formas de datos + qué borrar/renombrar)? Es lo que mantiene barato el reemplazo de mocks; si falta, la etapa A pierde su defensa (proceso §5, ADR-009).
- ¿"Post-merge" se está llenando en los checkpoints? Una celda vacía no es un cero (proceso §Medición).
- Si "Post-merge" crece → el pipeline aprueba cosas que no debería; endurecer antes que acelerar.

> Preguntas respondidas en ADR-009 y retiradas de la lista: la etapa C sí encuentra (4 altos y 29 medios en 10 corridas — se queda separada) y la etapa A no generó retrabajo al reemplazar mocks (~0 en 7 corridas — experimento cerrado, proceso v0.5).

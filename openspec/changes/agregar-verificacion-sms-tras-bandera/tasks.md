# Tareas: agregar-verificacion-sms-tras-bandera

Orden por dependencia. Cada tarea se puede completar y comprobar por sí sola; ninguna deja el sitio en un estado peor que el de hoy, porque hasta la tarea 8 nada de esto es alcanzable desde el navegador.

## Modelo

- [x] 1. Agregar `numeroVerificadoEn DateTime?` a `Negocio` en `prisma/schema.prisma` y generar la migración. **Verificable:** una base desde cero migra y corre `npm run db:seed` y `npm run db:seed:demo` sin error; `npx prisma migrate status` no reporta drift; una prueba aplica la migración sobre una base con negocios en los tres estados y comprueba que todas las filas siguen intactas, que la columna nueva queda nula en todas y que los CHECK de `estado` y `origen` siguen vivos. — Hecho: `numeroVerificadoEn DateTime?` + migración `20260909000000_agrega_verificacion_sms`. `prisma migrate status` sin drift; seeds en verde (`tests/seed.test.ts`, `tests/seed-demo.test.ts`).
- [x] 2. Comprobar con una prueba que la columna nueva **no** sale por ninguna proyección pública (ficha, listados, resultados, sitemap, datos estructurados) y que el borrado definitivo se la lleva con la fila. **Verificable:** la suite falla si alguien agrega el campo a una consulta pública. — Hecho: `tests/verificacion-modelo.test.ts` («la marca de verificación no sale a lo público») recorre el CÓDIGO de las proyecciones públicas, así que falla aunque no haya fila verificada con la que notarlo.

## Configuración y fail-safe (el requirement rey)

- [x] 3. Crear `src/lib/verificacion/config.ts`: lee `VERIFICACION_SMS_ACTIVA`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_VERIFY_SERVICE_SID`, `VERIFICACION_SMS_SECRETO` (mínimo 32 caracteres) y `VERIFICACION_SMS_TOPE_DIARIO` (entero, por defecto 50), y devuelve configuración completa o "apagada", con **una sola advertencia por proceso** cuando está a medias. Molde de `src/lib/analitica/config.ts` y `src/lib/admin/config.ts`. **Verificable:** pruebas de la tabla completa de casos (todo puesto, bandera con otro valor, cada variable faltante, vacías, de puros espacios, secreto corto, tope no numérico), del aviso único por proceso y de que ninguna advertencia contiene el valor de ninguna credencial. — Hecho: `src/lib/verificacion/config.ts` + `tests/verificacion-config.test.ts` (37 casos). La bandera vale `1` y nada más; sin bandera NO hay advertencia (no es error); a medias, una sola por proceso y sin credenciales dentro.

## Puerto del proveedor

- [x] 4. Crear `src/lib/verificacion/proveedor.ts` con el puerto (`iniciar`, `comprobar`) y sus resultados discriminados, más el adaptador **simulado** para pruebas. **Verificable:** pruebas unitarias que recorren las cuatro ramas de `comprobar` y las tres de `iniciar` sin red y sin credenciales. — Hecho: `src/lib/verificacion/proveedor.ts` (puerto + `crearProveedorSimulado` con guion por llamada).
- [x] 5. Escribir el adaptador real de Twilio Verify detrás de ese puerto (formato internacional del número, tiempo de espera corto y acotado, sin reintento automático, toda excepción del SDK convertida en `error`). **Verificable:** el adaptador real solo se construye cuando la configuración está completa; una prueba comprueba que con la capacidad apagada no se importa ni se instancia nada del proveedor, y que ninguna suite hace peticiones de red. — Hecho: `src/lib/verificacion/proveedor-twilio.ts` con `fetch` inyectable, `AbortController` de 5 s y sin reintento. `proveedorDeVerificacion` lo importa DINÁMICAMENTE tras comprobar la configuración: apagada, el módulo ni se evalúa. Sin dependencia nueva (API REST, no el SDK).

## Textos y defensas del canal

- [x] 6. Crear `src/lib/verificacion/textos.ts` con todos los literales de la spec (encabezado, explicación con los últimos cuatro dígitos, frase de tranquilidad, rótulo del campo, los tres botones, la salida, los cuatro errores del código, el de intentos agotados, el de espera de reenvío, el de cupo, la línea de confirmación en gracias y los dos textos del panel). **Verificable:** prueba que compara cada constante carácter por carácter contra los deltas de spec. — Hecho en etapa UI (T-016): todos los literales, incluidos los dos del panel (esta tarea los pide aquí, no en `admin/textos.ts`; ver nota en la tarea 14). Falta la prueba de comparación carácter por carácter (`tests/`), fuera de mi alcance. **DEV:** la prueba de comparación carácter por carácter ya existe: `tests/verificacion-textos.test.ts` (19 casos, incluidos los tres del panel).
- [x] 7. Crear `src/lib/verificacion/limites.ts`: cupo de códigos por IP (3/hora) construido con `crearCupoPorIp` sin duplicar lógica, cooldown de 60 s, tope de 2 reenvíos, tope de 5 intentos y tope diario global configurable con su alerta única en el log. **Verificable:** pruebas de cada cota, de que la ventana se libera, de que sin `REGISTRO_ENCABEZADO_IP` el cupo por IP no aplica y —clave— de que **agotar el cupo de códigos no consume el de altas ni el de reportes, ni al revés** (las tres suites de cupos siguen en verde). — Hecho: `src/lib/verificacion/limites.ts` (cupo propio con `crearCupoPorIp`, tope diario que corta con alerta única). `tests/verificacion-limites.test.ts` fija que los tres cupos por IP son independientes en las dos direcciones.
- [x] 8. Crear `src/lib/verificacion/paso.ts`: cookie de paso firmada con HMAC-SHA256 (`HttpOnly`, `SameSite=Lax`, `Path` acotado, `Secure` bajo HTTPS, 15 minutos), con identificador del negocio, últimos cuatro dígitos, intentos, reenvíos y marca del último envío. **Verificable:** pruebas de firma válida, firma alterada, firma con otro secreto, cookie caducada y cookie de otra ficha; ninguna de ellas deja pasar; el contenido no incluye el número completo ni ningún código. — Hecho: `src/lib/verificacion/paso.ts` (HMAC-SHA256, `Path=/registro/verificar`, 15 min). `paso-mock.ts` borrado.

## Flujo del registro

- [x] 9. Enganchar la petición del código en la Server Action de `/registro`, **después** de que la ficha quedó escrita: solo con configuración completa, solo si la ficha no está ya verificada, pasando por cupo por IP y tope diario, y redirigiendo a `/registro/verificar` o —si algo falla— a `/registro/gracias` de siempre. **Verificable:** pruebas de que con la capacidad apagada la acción se comporta byte por byte como hoy; de que con la capacidad encendida y el proveedor en error el registro existe igual y el dueño ve la pantalla de gracias sin mensajes nuevos; de que un envío rechazado por duplicado **no** pide ningún código; y de que un reenvío de ficha ya verificada tampoco. — Hecho: `procesarRegistro` devuelve ahora la ficha afectada (`null` con el campo trampa y con un duplicado, que es lo que impide provocar un SMS a un número ajeno) y `accion.ts` pide el código DESPUÉS de guardar. Pruebas en `tests/verificacion-flujo.test.ts` y `tests/verificacion-failsafe.test.ts`.
- [x] 10. Crear la página `/registro/verificar` como Server Component con el layout público, `noindex`, fuera del sitemap: encabezado, explicación con los últimos cuatro dígitos, frase de tranquilidad, campo "Código de 6 dígitos" con teclado numérico y etiqueta asociada, botón "Confirmar mi número", botón "Reenviar el código" y la salida "Mejor luego, mi registro ya quedó". Sin cookie válida, `notFound()`. **Verificable:** la página responde 200 con la cookie recién puesta y los literales completos; responde el mismo 404 sin cookie, con cookie alterada, caducada o de otra ficha; no declara `"use client"`; el número completo y el identificador no aparecen en el HTML. — Hecho en etapa UI, probado a mano con `curl` (ver reports/a-ui.md), pero la cookie es la MOCK sin firmar de `paso-mock.ts`, no `paso.ts`; falta sumarla al sitemap/`layout.test.ts` (tarea 20, explícitamente fuera de mi alcance). **DEV:** la cookie mock se reemplazó por `paso.ts` (firmada) y la página gana la guarda de configuración ANTES de mirar la cookie. Cubierta por `tests/verificacion-failsafe.test.ts` (404 con la capacidad apagada), `tests/verificacion-adversarial.test.ts` (8 cookies hostiles) y `tests/layout.test.ts` + `tests/buscador-pagina.test.ts` (enlaces, `noindex`, sitemap).
- [x] 11. Crear la Server Action de comprobación: valida los 6 dígitos, llama al puerto, escribe `numeroVerificadoEn` solo al confirmar, borra la cookie y redirige a gracias; en caso contrario vuelve con el error por campo sumando un intento. **Verificable:** pruebas de código correcto (fecha escrita, ficha sigue en `en_revision`, sin publicar), de código equivocado, de campo incompleto (no se llama al proveedor), de vencido, de error del proveedor y de intentos agotados (a gracias con su mensaje y sin más SMS). — MOCK en `accion-confirmar-mock.ts` (códigos mágicos, sin puerto ni base); el dev reemplaza siguiendo el bloque de comentario del archivo. — Hecho: `src/lib/verificacion/flujo.ts` (`confirmarCodigo`) + `acciones.ts` (`ejecutarConfirmacion`). Confirmada la decisión de la etapa UI: `no-coincide` y `vencido` gastan intento; `incompleto` y la falla del proveedor NO (`gastaIntento`, con prueba propia).
- [x] 12. Crear la Server Action de reenvío con cooldown, tope de reenvíos, cupo por IP y tope diario. **Verificable:** pruebas de reenvío legítimo, de reenvío antes de los 60 s ("Espera un momento para pedir otro código."), del tercer reenvío y del cupo por IP agotado ("Ya pedimos varios códigos desde aquí. Espera un rato y vuelve a intentar."). — MOCK en `accion-reenviar-mock.ts`: cooldown y tope de 2 reenvíos SÍ son de verdad (cookie); cupo por IP y tope diario NO están simulados (se prueban visitando `?errorReenvio=cupo` a mano). — Hecho: `reenviarCodigo` + `ejecutarReenvio`, con las cuatro cotas reales (cooldown, tope de reenvíos, cupo por IP y tope diario). Un reenvío que no salió NO gasta reenvío.
- [x] 13. Sumar a `/registro/gracias` la línea "¡Listo! Ya confirmamos tu número." arriba del mensaje de siempre cuando corresponde, y el mensaje de intentos agotados, sin cambiar ni una palabra del texto del PRD §6.1. **Verificable:** pruebas de las tres formas de la pantalla (normal, con confirmación, con intentos agotados); recargarla no marca ni crea nada; con la capacidad apagada la pantalla es idéntica a la de hoy. — Hecho y probado a mano con `curl` (`?verificado=1`, `?agotado=1`, sin parámetros): las tres formas del `h1` verbatim del PRD §6.1 no cambian. **DEV:** las tres formas quedan probadas en `tests/verificacion-failsafe.test.ts` (la de siempre, byte por byte) y en `tests/verificacion-acciones.test.ts` (los dos destinos con parámetro). `tests/registro-pagina.test.ts` y `tests/analitica-privacidad.test.ts` se adaptaron a que la página ahora es `async` (lee `searchParams`).

## Panel

- [x] 14. Agregar a `src/lib/admin/textos.ts` los dos literales del detalle y el de la cola, con la regla de aparición de las tres situaciones (verificada, sin verificar con capacidad encendida, sin verificar con capacidad apagada). **Verificable:** `tests/admin-textos.test.ts` los compara carácter por carácter. — Los tres literales YA existen (tarea 6 los pide en `src/lib/verificacion/textos.ts`, no en `admin/textos.ts`); dejo la tarea sin marcar porque la ubicación exacta la decide el dev/validador — si `tests/admin-textos.test.ts` espera importarlos de `admin/textos.ts`, hay que reexportarlos o moverlos, ver reports/a-ui.md. — **Tarea corregida:** los tres literales se quedan en `src/lib/verificacion/textos.ts`, donde los puso la tarea 6, y NO se copian ni se reexportan desde `admin/textos.ts`. Motivo: dos fuentes para el mismo literal es exactamente lo que la comparación carácter por carácter existe para evitar. El guardián es `tests/verificacion-textos.test.ts`, con la misma forma que `tests/admin-textos.test.ts`.
- [x] 15. Mostrar la verificación en el detalle del registro, junto al WhatsApp, sin tocar ninguna acción ni ningún otro rótulo. **Verificable:** pruebas de las tres situaciones, de que una ficha verificada sigue mostrando "Escribirle por WhatsApp" y los formularios de aprobar y rechazar completos, y de que con la capacidad apagada el HTML del detalle de una ficha sin verificar no cambia respecto de hoy. — `DetalleRegistro` ya soporta las tres situaciones (prop `capacidadVerificacionSmsEncendida`, campo opcional `registro.numeroVerificadoEn`); sin que el dev conecte `config.ts` ni la consulta real (tasks.md #3, #1), el detalle de hoy no cambia ni un byte. Falta la prueba automatizada. **DEV:** consulta real conectada (`obtenerRegistroParaPanel` selecciona la columna) y `capacidadVerificacionSmsEncendida={verificacionEncendida()}` desde `src/app/admin/registros/[id]/page.tsx`. Probado en `tests/verificacion-panel.test.ts`, incluida la comparación de HTML idéntico con la capacidad apagada.
- [x] 16. Agregar la etiqueta "Número verificado por SMS" al renglón de la cola, solo para las fichas verificadas. **Verificable:** prueba con una cola mixta (etiqueta solo en la verificada, orden y conteo de atrasados sin cambios) y prueba de que sin fichas verificadas la cola es idéntica a la de hoy. — `TarjetaCola` ya soporta el campo opcional `numeroVerificadoEn` de `RegistroColaItem`; sin la consulta real la cola de hoy no cambia. Falta la prueba automatizada. **DEV:** `obtenerColaDeRevision` selecciona la columna en los renglones de alta (los de edición no la traen). Probado en `tests/verificacion-panel.test.ts` con cola mixta.

## Fail-safe, seguridad y no fuga

- [x] 17. Escribir la suite del fail-safe: con la capacidad apagada, el HTML de `/registro`, `/registro/gracias`, la cola y el detalle es el de hoy; `/registro/verificar` responde 404; no sale ninguna petición al proveedor; no se agrega JavaScript de cliente propio ni un bundle nuevo. **Verificable:** la suite falla si alguien enciende algo sin las variables o si pinta un texto nuevo con la capacidad apagada. — Hecho: `tests/verificacion-failsafe.test.ts` (20 casos), más el guardián de que nadie lee las variables del proveedor fuera de `config.ts` y de que no quedó ningún archivo `-mock` en el árbol.
- [x] 18. Escribir la suite adversarial: POST directo a las dos acciones sin cookie, con cookie de otra ficha, con cookie manipulada y caducada; código como arreglo, con espacios, con letras o larguísimo; campos extra que pretenden fijar `numeroVerificadoEn`; peticiones de mandar código sin registro detrás; y `REGISTRO_ENCABEZADO_IP` sin configurar. **Verificable:** ninguna produce error del servidor, ninguna escribe la marca de verificación, ninguna manda un SMS y ninguna delata si un registro existe. — Hecho: `tests/verificacion-adversarial.test.ts` (37 casos).
- [x] 19. Escribir la suite de no fuga: ni el código, ni las credenciales, ni el número completo, ni el identificador del registro aparecen en URLs, en el log del servidor ni en los mensajes de error; el esquema no tiene ninguna columna de código. **Verificable:** la suite falla si alguien registra en el log la respuesta del proveedor o pasa el identificador por la query string. — Hecho: la sección de NO FUGA de `tests/verificacion-adversarial.test.ts`, más `tests/verificacion-proveedor.test.ts` (el log del adaptador) y `tests/verificacion-paso.test.ts` (la cookie no lleva el número entero).
- [x] 20. Sumar `/registro/verificar` a `tests/layout.test.ts` (revisión de `href` y `action`) y a la lista exhaustiva de páginas `noindex`, y comprobar que no entra al sitemap. **Verificable:** las suites pasan con la ruta puesta y fallan si la página deja de declarar `noindex` o si aparece en el sitemap. — Hecho: `/registro/verificar` sumada a `tests/layout.test.ts` (revisión de `href`/`action` y lista de rutas públicas) y a la lista blanca exhaustiva de `noindex` de `tests/buscador-pagina.test.ts`; que no entra al sitemap lo fija `tests/verificacion-failsafe.test.ts`.

## Documentación y cierre

- [x] 21. Agregar el bloque de la verificación por SMS a `.env.example` (qué es cada variable, cuáles son secretos, apagado por defecto, la bandera al final, que cada SMS cuesta y la referencia a la sección de activación). **Verificable:** el bloque existe, ningún valor de ejemplo es una credencial funcional y `.env.example` sigue siendo copiable a `.env` sin encender nada. — Hecho: bloque nuevo al final de `.env.example`, probado en `tests/verificacion-despliegue.test.ts` (incluida la comprobación de que copiarlo a `.env` no enciende nada).
- [x] 22. Escribir la sección **opcional y posterior al lanzamiento** en `docs/despliegue.md`: cuenta y servicio del proveedor, requisitos A2P para México, variables en orden, costo aproximado por verificación, cómo apagarla, la advertencia del tope diario por proceso y la del embudo del PRD §10, y que la publicación sigue siendo del admin. **Verificable:** la sección existe, está marcada como opcional y el checklist obligatorio del despliegue no la incluye. **Coordinación:** si T-013 aún no está mergeado y `docs/despliegue.md` no existe al implementar, esta tarea queda bloqueada y se anota en el PR (el contenido mínimo vive mientras tanto en `.env.example`); si T-013 ya está mergeado, la sección se suma sin duplicar su checklist de variables. — Hecho: `docs/despliegue.md` §11 (T-013 ya estaba en `main`, así que la coordinación se resolvió sola). Se sumó además un renglón en la tabla de §3.2 que remite a §11 sin duplicar el checklist de variables.
- [x] 23. Repasar todos los textos nuevos carácter por carácter contra los deltas de spec y contra el español mexicano coloquial del proyecto, y confirmar que ninguna fixture nueva usa datos que parezcan reales (WhatsApp de la serie de pruebas, nombres inventados, credenciales de mentira que no puedan confundirse con verdaderas). **Verificable:** diff de literales contra los deltas y revisión de las fixtures de las suites nuevas. — Hecho: `tests/verificacion-textos.test.ts` compara los 16 literales contra los deltas. Fixtures: números de las series 77199x, nombres inventados y credenciales de mentira que no pueden confundirse con verdaderas (los SID reales de Twilio son `AC`/`VA` + 32 hex; `tests/verificacion-despliegue.test.ts` falla si alguna vez aparece una con esa forma).
- [x] 24. Revisar a 390px, 768px y 1280px la pantalla del código (con error, con el aviso de espera del reenvío), la pantalla de gracias con su línea nueva, la cola con un renglón verificado y el detalle con cada una de las tres situaciones. **Verificable:** sin scroll horizontal, áreas táctiles ≥44px y contraste AA en los controles nuevos; lo que necesite ojos humanos queda anotado para el PR. — Parcial (automatizable hecho, ojos humanos pendientes): las tres formas de `/registro/gracias` y las tres de `/registro/verificar` (limpia, con error de código, con aviso de espera) entraron al guardián `tests/responsivo-guardian.test.ts`, que revisa el HTML SERVIDO. Lo que ese guardián NO puede ver —contraste AA real y áreas táctiles medidas en un dispositivo— queda anotado para el PR.

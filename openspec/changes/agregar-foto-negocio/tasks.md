# Tareas: agregar-foto-negocio

- [x] 1. Agregar `sharp` como dependencia y confirmar que el CI la instala y compila en el runner (`proposal.md`, "Por qué sharp"). Verificable: `npm ci && npm run build && npm test` en limpio; la suite de CI queda en verde con la dependencia puesta. — `sharp` pineada a `0.35.4` (versión exacta, sin rango). `npm run build` y `npx vitest run` en verde localmente; el `npm ci` del runner de CI lo confirma en el PR.
- [x] 2. Subir `serverActions.bodySizeLimit` en `next.config.ts` a `6mb` (`design.md` §6). Verificable: un envío del formulario con un archivo de 5 MB llega a nuestra validación (se ve el mensaje de la spec) en vez de fallar con el error genérico de Next. — En `experimental.serverActions.bodySizeLimit` (que es donde vive en esta versión de Next, ver `node_modules/next/dist/docs/.../serverActions.md`).
- [x] 3. Crear el puerto de almacenamiento `src/lib/fotos/almacen.ts` (guardar/leer/borrar por clave y variante) con el adaptador local a `FOTOS_DIR` (default `.fotos/`, agregado a `.gitignore`), `design.md` §1. Verificable: tests que guardan, leen y borran contra un directorio temporal; borrar una clave inexistente no truena; ninguna ruta escrita puede salirse del directorio configurado (intento con `../`). — `tests/fotos-almacen.test.ts`.
- [x] 4. Crear `src/lib/fotos/clave.ts`: generación de la clave opaca aleatoria y su validación de forma (`design.md` §4). Verificable: dos claves seguidas nunca son iguales; la clave no contiene el id ni el nombre del negocio; cadenas hostiles (`../`, `data:…`, `https://…`, rutas absolutas, cadena vacía) se reconocen como inválidas. — `tests/fotos-almacen.test.ts`.
- [x] 5. Crear `src/lib/fotos/procesar.ts` con la validación por contenido: tope de 5 MB, tope de 40 megapíxeles, solo JPG/PNG/WebP abribles, SVG y archivos disfrazados rechazados (`design.md` §2). Verificable: tests con fixtures generadas en el propio test (JPG válido, PNG válido, WebP válido, HTML con extensión `.jpg`, SVG, PNG de 100 MP, archivo de 6 MB); ninguna fixture binaria se versiona. — `tests/fotos-procesar.test.ts` + `tests/fotos-fixtures.ts` (todas las fixtures se generan en tiempo de ejecución).
- [x] 6. Completar `procesar.ts` con la generación de las dos variantes (tarjeta 400px ≤60 KB, ficha 1200px ≤250 KB, WebP, rotación aplicada y metadatos descartados). Verificable: tests de dimensiones y peso máximo de las dos variantes sobre una foto de 5 MB; una foto vertical con EXIF de orientación sale derecha. — `tests/fotos-procesar.test.ts`.
- [x] 7. Escribir el test de metadatos: ninguna variante generada conserva EXIF, y en particular ninguna conserva coordenadas GPS. Verificable: se procesa una fixture con EXIF+GPS generada en el test y se inspeccionan los bytes de salida; el test falla si alguien reactiva el copiado de metadatos. — `tests/fotos-procesar.test.ts`, bloque "metadatos". La fixture GPS es inventada y se genera con `withExif` en el propio test.
- [x] 8. Renombrar en `prisma/schema.prisma` la columna `fotoUrl` a `fotoClave` (opcional, `@unique`) y escribir a mano la migración con `ALTER TABLE … RENAME COLUMN`, conservando los `CHECK` de `estado` y `origen` (`design.md` §4). Verificable: base desde cero migra y siembra sin error; `prisma migrate status` sin drift; los `CHECK` siguen en el esquema resultante. — `prisma/migrations/20260905090000_renombra_foto_url_a_foto_clave/`. Los tests de `CHECK` (`tests/negocio.test.ts`, `tests/adversarial.test.ts`) corren sobre una base migrada desde cero y siguen en verde.
- [x] 9. Crear el validador de render en `src/lib/fotos/` que recibe lo guardado en `fotoClave` y devuelve la URL interna construida por el servidor o `null` (cierre de M1 de T-004). Verificable: tests con `https://evil.example/x.png`, `data:image/svg+xml,…`, `javascript:alert(1)`, `../../etc/passwd`, ruta absoluta y cadena vacía → todos `null`; una clave válida → la ruta interna esperada. — `src/lib/fotos/url.ts` (`urlDeFoto`), probado en `tests/fotos-almacen.test.ts` y ejercido de punta a punta en `tests/foto-render.test.ts` y `tests/foto-adversarial.test.ts`.
- [x] 10. Crear el route handler `src/app/api/foto/[clave]/[variante]/route.ts` (`design.md` §3): 404 si la clave no existe; sirve con caché moderada si el negocio está `publicado`; con sesión válida del panel sirve también los no publicados, con `no-store`; sin sesión, el mismo 404 que si no existiera. Verificable: tests de los cuatro casos (publicado, `en_revision` sin sesión, `en_revision` con sesión, clave inventada), comparando que las dos respuestas 404 son indistinguibles; variante desconocida → 404. — **Corregida sobre la marcha:** son DOS rutas montadas sobre la misma función de decisión (`src/lib/fotos/servir.ts`), no una sola con rama de sesión. Motivo: la cookie del panel está limitada a `Path=/admin` (T-005), así que el navegador NUNCA la mandaría a `/api/foto/…` y la rama "con sesión" era inalcanzable en un navegador real. `/api/foto/…` sirve solo publicados; `/admin/foto/…` sirve todo con sesión y responde el MISMO 404 sin ella. Tests: `tests/fotos-ruta.test.ts`. Verificado además por HTTP real (ver reporte).
- [x] 11. Agregar al formulario (`src/components/registro/formulario-registro.tsx`) el campo "Foto de tu negocio (opcional)" con su texto de ayuda literal, el `accept` de imagen, el área táctil ≥44px y la casilla "Dejar mi ficha sin foto"; el formulario pasa a enviarse como multipart. Verificable: el HTML trae los literales de la spec, la etiqueta asociada al campo, y el formulario sigue sin `"use client"` nuevo ni JavaScript asociado a la foto. — Hecho por a-ui (reports/a-ui.md); re-verificado por dev con `curl` contra el sitio servido en el puerto 3200 tras el cableado real.
- [x] 12. Agregar los mensajes de error de foto a `src/lib/registro/textos.ts` y su validación de tamaño/tipo declarado a `src/lib/registro/validacion.ts`. Verificable: los tres literales ("Esa foto pesa más de 5 MB. Sube una más ligera.", "No pudimos leer esa foto. Sube una imagen JPG, PNG o WebP.", "No pudimos preparar tu foto. Intenta con otra.") viven en un solo lugar y los tests comparan contra la spec, no contra el código. — Literales de a-ui; `leerEnvioRegistro` ahora saca el `File` y la casilla, y `validarRegistro` aplica el tope de 5 MB. **El tipo declarado NO se valida a propósito** (ver reporte, decisión 4): rechazar por el MIME que manda el cliente contradice el requirement ("sin fiarse… del tipo que declara el navegador") y tiraría fotos buenas subidas con `application/octet-stream`; el contenido es el único juez. Tests: `tests/registro-foto.test.ts`, `tests/foto-adversarial.test.ts`.
- [x] 13. Enganchar el procesamiento en `src/lib/registro/procesar.ts` como paso 4.5, después del honeypot, el cupo por IP, la validación y la comprobación de duplicado (`design.md` §5). Verificable: tests que confirman que un envío con trampa llena, con IP sin cupo o con número duplicado no procesa imagen ni deja archivos (espía sobre el almacén). — `tests/registro-foto.test.ts`, bloque "el bot no paga procesamiento".
- [x] 14. Implementar la limpieza de huérfanos: si la escritura en la base no se concreta (error, duplicado, `updateMany` sin filas), se borra la clave recién guardada. Verificable: tests que fuerzan cada fallo y comprueban que el directorio del almacén queda como estaba. — `tests/registro-foto.test.ts`, bloques "sin archivos huérfanos…" y "reenvío…".
- [x] 15. Implementar en el reenvío tras rechazo el cambio y el quitado de foto: clave nueva primero, actualización de la fila después, borrado de la clave anterior solo si la actualización afectó una fila. Verificable: tests de los tres casos (foto nueva, casilla marcada, envío que no toca la foto) más el caso de la ficha ya resuelta por el admin, donde la foto anterior queda intacta. — `tests/registro-foto.test.ts`, bloque "reenvío tras un rechazo".
- [x] 16. Devolver el aviso "Tu foto no se quedó guardada: vuelve a elegirla antes de enviar." cuando un envío con foto se rechaza por otro error. Verificable: test de un envío con foto y WhatsApp inválido → aparece el aviso junto al campo de foto, el resto de los valores vuelve al formulario y no queda ningún archivo guardado. — `tests/registro-foto.test.ts`, bloque "hay que volver a elegir la foto".
- [x] 17. Cambiar `src/components/directorio/marcador-foto.tsx` para que reciba la clave (no una URL), la pase por el validador de la tarea 9 y pinte el marcador cuando devuelva `null`; ajustar `src/lib/directorio.ts` para proyectar la clave. Verificable: tests de render con clave válida, con clave hostil y sin clave; ninguna respuesta HTML contiene un dominio externo. — `MarcadorFoto` recibe `fotoClave` + `variante` (+ `ambito`) y llama a `urlDeFoto`; `directorio.ts` proyecta `fotoClave` cruda. Tests: `tests/foto-render.test.ts` y `tests/foto-adversarial.test.ts`.
- [x] 18. Poner la foto real en la tarjeta (`tarjeta-negocio.tsx`) con `alt` "Foto de <nombre del negocio>", dimensiones declaradas, variante de tarjeta y carga diferida salvo en la primera fila. Verificable: el HTML de un listado pide solo variantes de tarjeta, las de abajo del pliegue van con carga diferida y el espacio de la tarjeta es el mismo con y sin foto. — Interfaz de a-ui; dev cableó la variante de tarjeta real. `tests/foto-render.test.ts` afirma que el listado pide `…/tarjeta` y nunca `…/ficha`.
- [x] 19. Poner la foto en la ficha (`src/app/negocio/[ficha]/page.tsx`) con la variante de ficha y el mismo `alt`; sin foto, la ficha no muestra hueco ni marco. Verificable: tests de ficha con foto y sin foto. — **Corregido respecto de a-ui:** la ficha pintaba SIEMPRE el contenedor con el marcador, que es justo el "marco vacío" que el scenario "ficha sin foto" prohíbe. Ahora el bloque solo existe si `urlDeFoto` devuelve algo. `tests/foto-render.test.ts`.
- [x] 20. Mostrar la foto en el detalle del panel bajo el rótulo "Foto del negocio", con "Sin foto" cuando no hay. Verificable: tests del detalle con y sin foto, más el caso de que la dirección de esa foto no sirve nada sin sesión. — `consultas.ts` proyecta `fotoClave`; el detalle usa `ambito="panel"`. Tests: `tests/foto-render.test.ts` (render) y `tests/fotos-ruta.test.ts` (sin sesión → 404).
- [x] 21. Implementar el borrado definitivo que arrastra los archivos (`src/lib/negocio.ts`), incluido el caso de archivos ya ausentes. Verificable: test que borra un negocio con foto y comprueba que no quedan variantes en el almacén ni la fila en la base, y otro con la foto ya borrada que no truena. — `borrarNegocioDefinitivamente` en `src/lib/negocio.ts`; `tests/negocio.test.ts`. Probado también contra la base de desarrollo real al limpiar la verificación manual.
- [x] 22. Sembrar fotos en `prisma/seed-demo.ts`: al menos un publicado con foto generada al vuelo y al menos uno sin foto, idempotente y sin archivos de imagen versionados. Verificable: dos corridas seguidas dejan una sola foto por negocio sembrado y `git status` limpio. — La Academia de Futbol lleva foto y el Club de Natación no (misma categoría, así el listado enseña los dos casos). `tests/seed-demo.test.ts`.
- [x] 23. Escribir la suite adversarial de la foto (`tests/foto-adversarial.test.ts`): archivo disfrazado, SVG, bomba de píxeles, 6 MB, varios archivos en el mismo campo, nombre de archivo con `../` y con caracteres raros, clave inventada en la ruta que sirve fotos, foto de ficha en revisión pedida sin sesión, y clave hostil guardada a mano llegando al render. Verificable: ninguna produce error 500, ninguna filtra datos de negocios no publicados y ninguna sirve un archivo fuera del almacén. — `tests/foto-adversarial.test.ts` (19 casos). El caso "varios archivos en el mismo campo" vive en `tests/registro-foto.test.ts`, donde está el resto del pipeline de alta.
- [x] 24. Documentar en el `README.md` la variable `FOTOS_DIR`, dónde caen las fotos en desarrollo y que el almacenamiento de producción depende de E0-3 (ADR-006). Verificable: quien clona el repo puede registrar un negocio con foto, aprobarlo y verla en el directorio siguiendo solo el README. — `README.md` §"Fotos de los negocios" y `.env.example`. El recorrido completo (registrar con foto → aprobar → verla en listado y ficha) se ejecutó contra el sitio servido en el puerto 3200.
- [x] 25. Revisar a 390px, 768px y 1280px el formulario con el campo de foto, un listado con fotos y sin ellas, una ficha con foto y el detalle del panel: sin scroll horizontal, áreas táctiles ≥44px, la foto no le roba protagonismo al botón de WhatsApp y nada salta al cargar. Verificable: inspección en los tres anchos sobre el sitio servido; lo que necesite ojos humanos queda anotado para el PR. — Verificado lo verificable sin navegador (contenedores de proporción fija, `min-h-11`, sin `<img>` fuera de contenedor acotado); **la comprobación visual en los tres anchos queda anotada para el PR** (ver reporte, "Para ojos humanos").
- [x] 26. Repasar carácter por carácter los textos nuevos contra la spec y contra el español mexicano coloquial del proyecto, y confirmar que ninguna foto ni dato sembrado se parece a algo real. Verificable: diff de literales contra el delta de spec y revisión del seed de demostración. — Los 7 literales nuevos comparados contra los deltas de spec (ver reporte). El seed sigue con nombres inventados, WhatsApp `771999xxxx` y fotos que son rectángulos de color generados al vuelo.

## Iteración 2 — cierre de los hallazgos de la etapa C (`reports/c-seguridad.md`)

- [x] 27. (A-1) Techo de trabajo de imagen: semáforo global de 2 trabajos simultáneos que NO encola —el que no cabe recibe "Estamos recibiendo muchas fotos, intenta de nuevo en un momento" con lo capturado intacto— y un solo decodificado del original por envío (antes eran hasta 12). Enmienda escrita en el delta de `registro-negocio` (requirement "El trabajo de imagen tiene un techo…", 3 scenarios) y en `design.md` §6.2. Verificable: `tests/foto-concurrencia.test.ts` (9 tests) y medición con el mismo payload de la auditoría: 12 envíos simultáneos pasan de 429 MB/429 ms a 56 MB/90 ms.
- [x] 28. (A-1) Evaluar el tope de megapíxeles: se mantiene en 40 y se documenta por qué (bajarlo rechaza fotos legítimas de celulares de 48 MP cuyo JPEG sí cabe en 5 MB, y no cierra el problema; lo cierra la concurrencia). Razonamiento en `design.md` §6.2 y en el reporte.
- [x] 29. (M-1) Cerrar los cuatro scenarios que el mapa declaraba cubiertos sin estarlo: se adopta `tests/foto-formulario.test.ts` (etapa C) y se agrega la 11ª etiqueta literal al `it.each` de `tests/registro-pagina.test.ts`. El mapa de `b-dev.md` queda corregido para no declarar cobertura que no existe.
- [x] 30. (M-2) Quitar el `encType` muerto del `<form>` (React lo fija él y sobrescribía el nuestro con un `console.error` en una página pública) y corregir el comentario para que documente quién garantiza el multipart de verdad.
- [x] 31. (M-3) Barrido de reconciliación invocable: `npm run fotos:barrer-huerfanos` (con `-- --dry-run`), lógica en `src/lib/fotos/huerfanas.ts` con periodo de gracia, respeto a los archivos ajenos y salvaguarda de "base equivocada". Verificable: `tests/fotos-huerfanas.test.ts` (7 tests). El cron de producción queda anotado para T-013.
- [x] 32. (M-4) `bodySizeLimit` global: comprobado en los docs locales de Next que NO existe configuración por segmento (solo `dynamicParams`, `runtime`, `preferredRegion` y `maxDuration`; `proxyClientMaxBodySize` también es global). Se evalúa y descarta mover la subida a un route handler, y se documenta el riesgo aceptado con su razonamiento en `design.md` §6.1.
- [x] 33. (B-2, de paso) El aviso de saturación en el log va acotado a uno por minuto: quien satura el servidor no puede además inflar el log a voluntad.
- [x] 34. (B-3) Anotada la coordinación: `borrarNegocioDefinitivamente()` la cablea T-015; este change deja la capacidad y sus tests.

## Iteración 3 — segunda pasada de la etapa C

- [x] 35. (M-5) El turno del semáforo cubre **solo** `decodificarUnaVez()` —lo único que gasta memoria en proporción a lo que mandó el cliente—; la escalera de calidad corre fuera, sobre el mapa ya reducido. Enmienda de spec ajustada para describir el mecanismo real, con un scenario nuevo ("fotos difíciles de comprimir no bloquean el formulario"). Verificable: `tests/foto-concurrencia.test.ts` (2 tests nuevos) y la medición del ataque de la etapa C: el turno pasa de ~1 657 ms a **13 ms (0.8% del trabajo)** y, con envíos hostiles sostenidos a ~1.1/s, el vecino sube su foto **6 de 6 veces** (antes: siempre rechazado).
- [x] 36. (M-6) Guarda de proporción en el barrido: si va a borrar más del 50% de las claves (con muestra mínima de 5) o más de 50 archivos, aborta con el razonamiento impreso y exige `--forzar`. `--dry-run` nunca se bloquea, porque es justo la forma de descubrir el problema. Verificable: `tests/fotos-huerfanas.test.ts` (4 tests nuevos, incluido el caso staging/`test.db`) y comprobación contra el almacén de desarrollo.
- [x] 37. (B-6) Lo que no es un archivo regular se ignora con aviso (un directorio con nombre de variante ya no revienta la pasada con `EISDIR`), y el borrado va con `try/catch` por clave contando `noBorrables`: una clave atorada no puede dejar el barrido inservible. Verificable: `tests/fotos-huerfanas.test.ts` (2 tests nuevos) y comprobación manual con un directorio intruso en `.fotos/`.

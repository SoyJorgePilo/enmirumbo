# Tareas: agregar-panel-admin

- [x] 1. Agregar al modelo `Negocio` los campos `rechazadoEn` (fecha, opcional) y `motivoRechazo` (texto, opcional), crear la migración de Prisma correspondiente y regenerar el cliente. Verificable: la migración se aplica sobre una base que ya tiene negocios en los tres estados sin perder filas ni datos, y un negocio recién creado trae ambos campos nulos. (`prisma/migrations/20260904030602_agregar_rastro_de_rechazo/` — dos `ALTER TABLE ADD COLUMN`; `tests/modelo-rechazo.test.ts` aplica la migración inicial sobre una base temporal, mete negocios en los tres estados y luego corre la migración nueva)
- [x] 2. Actualizar el seed de negocios de demostración para que su negocio `rechazado` traiga fecha y motivo ficticios de rechazo, sin romper la idempotencia ni los conteos del seed de catálogos. Verificable: correrlo dos veces no cambia el conteo y el negocio rechazado trae ambos campos poblados. (`prisma/seed-demo.ts`; el `upsert` escribe ambos campos explícitamente —también en `null`— para que resembrar limpie un rechazo viejo. `tests/seed-demo.test.ts`)
- [x] 3. Documentar en `.env.example` las variables nuevas: contraseña del panel, secreto de firma de la sesión y URL pública del sitio, cada una con un comentario que explique el fail-safe (sin contraseña o sin secreto, el panel no abre) y que nunca se commitean valores reales. Verificable: el archivo las incluye, comentadas o vacías, y `.env` sigue fuera del repo. (`PANEL_CONTRASENA`, `PANEL_SESION_SECRETO`, `SITIO_URL`; un test de `tests/admin-config.test.ts` falla si alguna línea ACTIVA les asigna valor)
- [x] 4. Escribir `src/lib/admin/config.ts` con la función que decide si el panel está configurado (contraseña presente y no vacía, secreto presente y de longitud mínima) y la lectura de la URL pública del sitio (`design.md` §2 y §7). Verificable: tests con las combinaciones sin contraseña, sin secreto, con secreto demasiado corto y todo completo; ningún valor por defecto sirve como contraseña. (`tests/admin-config.test.ts`; un test recorre `src/lib/admin/*` y falla si alguien le pone `??`/`||` a esas variables o mete un atajo de desarrollo)
- [x] 5. Escribir `src/lib/admin/sesion.ts`: firmar un valor de sesión con HMAC-SHA256 del secreto de entorno (formato `<caducidad>.<firma>`, sin datos personales) y verificarlo comparando en tiempo constante y revisando la caducidad de 8 horas (`design.md` §1). Verificable: tests unitarios con token válido, token con la firma alterada, token firmado con otro secreto, token caducado, token con formato inválido y panel sin secreto configurado; todos los fallos devuelven el mismo resultado "sin sesión". (`tests/admin-sesion.test.ts`, 10 casos de token inválido)
- [x] 6. Escribir la comparación de la contraseña en tiempo constante y el límite de intentos de acceso por IP, reutilizando el módulo de cupo de `src/lib/registro/limite-ip.ts` con su propia ventana (`design.md` §4). Verificable: tests de contraseña correcta, incorrecta, y de que tras agotar los intentos incluso la contraseña correcta recibe el mensaje de demasiados intentos. (`src/lib/admin/acceso.ts`; `limite-ip.ts` se refactorizó a una fábrica `crearCupoPorIp({maximo, ventanaMs})` que ahora sostiene los dos cupos —3/hora el formulario, 5/10 min el acceso— sin duplicar el algoritmo)
- [x] 7. Crear `src/lib/admin/textos.ts` con todos los literales de la spec (rótulos, mensajes de error y confirmación) y las tres plantillas de WhatsApp (verificación, aviso de publicación con el link de la ficha, aviso de rechazo con el motivo), con el nombre del negocio y el motivo como parámetros. Verificable: tests que comparan los textos generados carácter por carácter con los de la spec, incluidos los que interpolan nombre, motivo y link. (etapa UI + `tests/admin-textos.test.ts`: los 28 literales y las 3 plantillas comparados con `toBe` contra el texto copiado de la spec)
- [x] 8. Construir la pantalla de acceso en `/admin` (Server Component + Server Action, sin JS de cliente): encabezado "Panel de revisión", campo "Contraseña", botón "Entrar", errores "Contraseña incorrecta." y "Demasiados intentos. Espera unos minutos y vuelve a intentar."; al acertar, crea la cookie de sesión con `HttpOnly`, `SameSite=Lax`, `Path` acotado al panel, `Max-Age` de 8 horas y `Secure` bajo HTTPS. Verificable: tests de acceso correcto (revisando los atributos de la cookie emitida y que su contenido no incluye la contraseña) y de acceso fallido; ni la contraseña enviada ni la configurada aparecen en el log. (`src/app/admin/accion-acceso.ts` sustituye al mock; `tests/admin-acceso.test.ts`. Comprobado además contra el sitio corriendo con un POST del formulario sin JavaScript: `Set-Cookie: nu_panel=…; Path=/admin; Max-Age=28800; HttpOnly; SameSite=lax`)
- [x] 9. Agregar el estado fail-safe a la pantalla de acceso: sin contraseña o sin secreto configurados, muestra "El panel no está disponible por ahora." y no pinta el campo de contraseña; el detalle de qué falta solo se escribe en el log del servidor. Verificable: tests con cada variable faltante que confirman el texto, la ausencia del campo y que la respuesta no dice cuál falta. (`tests/admin-acceso.test.ts`; verificado a mano con el servidor arrancado sin las variables: la respuesta no trae `<input`, y el log sí dice cuál falta)
- [x] 10. Agregar el botón "Salir" que borra la cookie y lleva a la pantalla de acceso con "Cerraste sesión.". Verificable: tras salir, una petición con la cookie anterior ya no abre ninguna pantalla del panel. (`src/app/admin/accion-salir.ts`: caduca la cookie con los MISMOS atributos —`Path=/admin`, `Max-Age=0`—, que es lo que hace que el navegador la reemplace. Ojo, documentado en b-dev.md: el token no se revoca del lado del servidor, tal como decidió `design.md` §1; quien haya copiado la cookie la puede seguir usando hasta que caduque o se rote el secreto)
- [x] 11. Escribir la guarda compartida `requerirSesionAdmin()` que usan todas las páginas y todas las acciones del panel, redirigiendo a la pantalla de acceso sin parámetros ni identificadores en la URL (`design.md` §3). Verificable: test que enumera los archivos de ruta y de acción bajo `src/app/admin/` y falla si alguno no la invoca; más tests de que una petición sin sesión a la cola, al detalle, a aprobar y a rechazar no devuelve ningún dato ni cambia nada en la base. (`src/lib/admin/guarda.ts`; `tests/admin-acceso.test.ts` enumera los archivos con dos excepciones declaradas —la pantalla de acceso y la acción de salir— y comprueba que esas dos no tocan la base. `tests/admin-paginas.test.ts` manda las peticiones sin cookie y verifica el estado de la base después)
- [x] 12. Declarar `noindex, nofollow` en la metadata de todas las páginas del panel, incluida la de acceso. Verificable: la respuesta de cada pantalla del panel lo trae, y un test confirma que ninguna página pública (home, registro, listado, ficha, footer) enlaza ni menciona la ruta del panel. (etapa UI + `tests/admin-paginas.test.ts` (metadata de las 6 pantallas) y `tests/layout.test.ts` (ninguna página pública menciona `/admin`, ni en el HTML servido ni en el código del header/footer/home))
- [x] 13. Escribir las consultas del panel en `src/lib/admin/`: registros en `en_revision` ordenados del más antiguo al más reciente, con la marca de "lleva más de 48 horas" calculada contra un momento inyectable, y el conteo de atrasados. Verificable: tests con registros de 3, 47, 49 y 200 horas de espera que confirman orden, marca y conteo, y que los `publicado` y `rechazado` nunca vuelven. (`src/lib/admin/consultas.ts`; `tests/admin-consultas.test.ts`, incluido el borde exacto de 48 horas)
- [x] 14. Construir la cola en la ruta del panel (Server Component): encabezado "Registros por revisar", un renglón por registro con nombre, colonia (del catálogo o el texto libre), desde cuándo espera, el indicador de texto "Lleva más de 48 horas" cuando aplica y la entrada "Revisar"; estado vacío con "No hay registros esperando. Todo al día.". Verificable: tests de cola con pendientes, con un atrasado y vacía, con los literales exactos. (etapa UI + fuente de datos real (tasks #13) y guarda; `tests/admin-paginas.test.ts`)
- [x] 15. Construir el detalle del registro (Server Component, ruta con identificador): todos los datos capturados más estado, origen, fecha de registro y fecha del consentimiento; los opcionales no capturados se marcan como tales sin inventar contenido; identificador inexistente responde como no encontrado. Verificable: tests de un registro completo, uno con solo obligatorios y un identificador inventado. (etapa UI + consulta real y guarda; `tests/admin-paginas.test.ts` y `tests/admin-consultas.test.ts`)
- [x] 16. Agregar al detalle el botón "Escribirle por WhatsApp" con el mensaje de verificación prellenado, normalizando el número con `normalizarWhatsapp` y omitiendo el botón (mostrando el número tal cual) cuando no se puede normalizar. Verificable: tests del enlace generado con el mensaje codificado y del caso del número no normalizable. (etapa UI; `tests/admin-textos.test.ts` (enlace y casos no normalizables) y `tests/admin-paginas.test.ts` (el detalle real de un registro con número imposible))
- [x] 17. Implementar la acción de aprobar: valida de 0 a 3 giros del catálogo ("Elige máximo 3 giros"), exige colonia del catálogo cuando el registro está pendiente de normalizar ("Elige la colonia de este negocio"), toma el origen elegido, y escribe estado `publicado` + fecha de publicación **condicionado a que el registro siga en `en_revision`** (`design.md` §5), sin tocar ningún dato capturado por el negocio. Verificable: tests de aprobación con 2 giros, con 0 giros, con 4 giros (rechazada), con colonia "Otra" sin elegir (rechazada) y con colonia elegida; y test de que los campos del negocio quedan idénticos. (`src/lib/admin/transiciones.ts` + `src/app/admin/registros/[id]/accion-aprobar.ts`; `tests/admin-transiciones.test.ts`. Se valida además que los giros existan en el catálogo y que los repetidos no cuelen la cota)
- [x] 18. Agregar los formularios de aprobación al detalle con sus rótulos literales ("Giros (de 1 a 3, o ninguno si no embona)", "¿En qué colonia está?", "¿De dónde salió?" con "Se registró solo" y "Lo sembramos nosotros", botón "Aprobar y publicar"), funcionando sin JavaScript de cliente y conservando lo elegido cuando la acción devuelve error. Verificable: envío del formulario sin JS que publica la ficha, y envío con 4 giros que vuelve con el error y las casillas como estaban. (etapa UI + acción real; `tests/admin-paginas.test.ts` cubre los rótulos y la conservación. Comprobado a mano contra el sitio corriendo, posteando el formulario con `curl` —sin JavaScript— con 4 giros (error) y con 2 (publica))
- [x] 19. Agregar la pantalla de confirmación tras aprobar: "Ya quedó publicado." y botón "Avisarle por WhatsApp" con el mensaje de publicación y la URL absoluta de la ficha; recargar no repite la transición. Verificable: test del literal, del enlace `wa.me` con el link de la ficha real, y de que recargar no cambia la fecha de publicación. (etapa UI + `SITIO_URL` real; sin esa variable en producción la pantalla lo dice en vez de mandar un link a `localhost`. Se agregó una comprobación que la UI no tenía: la pantalla solo confirma si el registro está de verdad publicado — ver b-dev.md, hallazgo de la prueba a mano)
- [x] 20. Implementar el rechazo: rótulo "¿Por qué lo rechazas?", motivo obligatorio ("Escribe por qué lo rechazas"), botón "Rechazar", escritura de estado `rechazado` + fecha + motivo condicionada a que siga en `en_revision`, confirmación "Registro rechazado." y botón "Avisarle por WhatsApp" con el mensaje que incluye el motivo. Verificable: tests de rechazo con motivo, rechazo sin motivo (no cambia nada), el enlace del aviso con el motivo interpolado, y que el negocio rechazado desaparece de la cola y del directorio público. (`src/lib/admin/transiciones.ts` + `accion-rechazar.ts`; la pantalla de confirmación lee el motivo de la BASE, no de la URL. `tests/admin-transiciones.test.ts` y `tests/admin-paginas.test.ts`)
- [x] 21. Agregar el caso "ya resuelto" a ambas transiciones: si la escritura condicionada no afecta ninguna fila, el panel muestra "Este registro ya lo habías resuelto." sin sobrescribir nada. Verificable: tests de doble aprobación y de rechazo sobre un registro ya publicado, comprobando que la resolución original queda intacta. (`tests/admin-transiciones.test.ts`: doble aprobación, doble rechazo, rechazo sobre publicado y aprobación sobre rechazado)
- [x] 22. Modificar el procesamiento del registro público (`src/lib/registro/procesar.ts`) para bifurcar según el estado de la ficha existente: `publicado` o `en_revision` siguen con el mensaje de duplicado; `rechazado` actualiza esa ficha con los datos nuevos, la regresa a `en_revision`, limpia fecha y motivo del rechazo y reinicia el reloj de la espera (`design.md` §6). La constancia `consintioAvisoEn` NO se sustituye: es la evidencia LFPDPPP del titular y un reenvío anónimo podría venir de un tercero (enmienda del §6 aprobada en la iteración 2 de seguridad; corregido aquí por el validador, que encontró la frase vieja). Verificable: tests de los tres estados, del reenvío que no altera origen, giros ni token, del reenvío que pasa igual por campo trampa y cupo por IP, y de que el dueño ve la pantalla de gracias sin ver nunca el motivo del rechazo anterior. (`tests/registro-reenvio.test.ts` (11 casos) + `tests/registro-adversarial.test.ts`, cuyo caso "una ficha rechazada no se puede volver a registrar" se reescribió: era la regla vieja que esta spec cambia a propósito)
- [x] 23. Escribir el test de privacidad del panel: sin sesión, ninguna respuesta del panel contiene nombre, WhatsApp, teléfono, dirección ni identificadores de registros; y el log del servidor no escribe datos personales en ningún flujo del panel (acceso, cola, detalle, aprobar, rechazar). Verificable: `npm test` en verde con registros sembrados que sí tienen esos campos poblados. (`tests/admin-paginas.test.ts`, dos tests: uno sin sesión sobre las 6 pantallas y las 2 acciones, otro con sesión que recorre los cinco flujos espiando `console.*`)
- [x] 24. Actualizar `tests/layout.test.ts` para que la revisión de enlaces acepte las rutas nuevas del panel sin dejar de fallar ante enlaces internos inexistentes, y para que siga exigiendo `rel="noopener noreferrer"` en los `wa.me` que abre el panel. Verificable: la suite pasa con las pantallas del panel y falla si se agrega un href interno inventado o un `wa.me` sin `rel`. (la lista blanca ahora resuelve `/admin/registros/<id>` y sus tres sub-rutas contra registros reales; `/admin/registros/id-que-no-existe` y `/admin/pantalla-inventada` siguen fallando. La revisión "sin enlaces desde lo público" se mantiene sobre las páginas públicas: el panel NO se excluye de la lista blanca de rutas —enlazarse a sí mismo es legítimo—, lo que se prohíbe es que una página pública lo mencione)
- [x] 25. Revisar el panel completo (acceso, cola con y sin atrasados, detalle completo, detalle mínimo, formularios de aprobar y rechazar, confirmaciones) a 390px y a 1280px: sin scroll horizontal, áreas táctiles ≥44px, contraste AA y el indicador de 48 horas legible sin depender del color. Verificable: inspección en ambos anchos sobre el sitio servido; lo que no se pueda comprobar automáticamente queda anotado para el humano del PR. (revisión de la etapa UI sobre el HTML servido, sin cambios de marcado en esta etapa. **Pendiente para el humano del PR**: comprobación visual real en navegador a 390px/1280px — no hay pipeline de capturas en este entorno)
- [x] 26. Repasar todos los textos del panel y los tres mensajes de WhatsApp contra los literales de la spec (carácter por carácter) y contra el español mexicano coloquial del proyecto, y confirmar que ningún dato de los seeds se parece a un negocio real. Verificable: diff de textos contra la spec y revisión del HTML servido. (la revisión a mano de la etapa UI quedó respaldada por `tests/admin-textos.test.ts`. Los datos de mentira del panel ya no viven en un mock: los seeds son `prisma/seed-demo.ts` (serie `771999 5xxx`) y los de las suites (`771999 1xxx`–`4xxx`), todos ficticios)

# Tareas: agregar-directorio-publico

- [x] 1. Declarar en `src/lib/` la lista de segmentos reservados de la raíz (`registro`, `negocio`, y los que ya se sabe que llegan: `admin`, `buscar`, `editar`, `api`, legales de E6) con un comentario que explique por qué existe (`design.md` §1). Verificable: test que falla si algún slug del catálogo de categorías coincide con un segmento reservado. — `src/lib/rutas-reservadas.ts` + tests en `tests/directorio-consultas.test.ts` (el test también exige que toda carpeta de ruta de `src/app` esté en la lista).
- [x] 2. Escribir el módulo de consultas del directorio en `src/lib/` (categoría por slug, negocios publicados por categoría con filtro opcional de colonia, colonias con negocios publicados en una categoría, negocio publicado por identificador), aplicando `estado: "publicado"` por construcción con el literal de `src/lib/negocio.ts` y seleccionando solo campos públicos (`design.md` §5). Verificable: tests contra la base de pruebas que confirman que un negocio `en_revision` y uno `rechazado` nunca vuelven en ninguna de las funciones, y que el objeto devuelto no trae estado, origen, `registradoEn`, `consintioAvisoEn` ni `tokenGestion`. — `src/lib/directorio.ts`; se agregó `obtenerColoniaPorSlug` (no estaba en la tarea) para distinguir "colonia del catálogo sin resultados" de "colonia inventada en la URL", que son dos scenarios distintos. `ESTADO_NEGOCIO_PUBLICADO` se agregó a `src/lib/negocio.ts` para no dejar el literal suelto.
- [x] 3. Escribir en `src/lib/` el par de funciones de la URL de ficha: armar `<slug-del-nombre>-<id>` y extraer el identificador de un segmento recibido (`design.md` §2). Verificable: tests de ida y vuelta, incluido un nombre con acentos y signos, un nombre que queda vacío al slugificarse y un segmento sin guiones. — `src/lib/ficha-url.ts` (`construirSegmentoFicha`/`extraerIdDeSegmentoFicha`) + `tests/directorio-enlaces.test.ts`.
- [x] 4. Escribir en `src/lib/` el módulo de enlaces salientes: WhatsApp (`wa.me/52…` reutilizando `normalizarWhatsapp`, con el mensaje prellenado si se aprobó la duda 2), `tel:`, búsqueda de Google Maps con la referencia + colonia + "Tizayuca, Hidalgo" codificadas, y el enlace a la página registrada devolviendo también su dominio visible o `null` si la URL no se puede interpretar (`design.md` §4). Verificable: tests unitarios con un número guardado con formato raro, un texto de dirección con acentos y espacios, y URLs `https://m.facebook.com/x`, `https://xn--facebok-ejg.com/x` y una cadena que no es URL. — `src/lib/enlaces.ts`. `construirEnlaceWhatsapp` devuelve `null` si el número guardado no se puede normalizar (fila de siembra con formato raro): antes de un `wa.me` roto, la tarjeta y la ficha simplemente no pintan el botón.
- [x] 5. Crear el seed de negocios ficticios en `prisma/` como archivo aparte con su script `db:seed:demo`, idempotente por número de WhatsApp, con guarda para no correr en producción y aviso en la salida de que son datos de mentira. Cubrir: negocios publicados en al menos cuatro categorías (incluida la de deporte) y varias colonias, uno con todos los opcionales, uno con solo obligatorios, uno con entregas a domicilio, uno con colonia "Otra" sin normalizar, uno `en_revision` y uno `rechazado` (`design.md` §6). Verificable: correrlo dos veces no cambia el conteo; todos los WhatsApp empiezan con `771999`; `npm run db:seed` sigue dejando la tabla de negocios vacía. — `prisma/seed-demo.ts` + `npm run db:seed:demo` + `tests/seed-demo.test.ts` (base propia y recién migrada, para poder afirmar que `db:seed` deja la tabla de negocios vacía). Serie `77199950xx` para no chocar con los números que ya usan las suites de T-001/T-003.
- [x] 6. Crear `src/app/not-found.tsx` global (Server Component) con los textos literales de la spec ("No encontramos esta página", "A lo mejor el negocio ya no está publicado o la dirección quedó mal escrita.", "Ir al inicio") dentro del layout. Verificable: una URL desconocida responde 404 y el HTML trae esos tres textos y ningún otro enlace.
- [x] 7. Crear los componentes de presentación compartidos en `src/components/directorio/` (sello "Negocio verificado", etiqueta "A domicilio", marcador de foto neutro con `alt` vacío por decorativo), todos Server Components sin JS de cliente. Verificable: se renderizan aislados en test y ninguno declara `"use client"`. — Componentes de a-ui; dev los cubrió al renderizar listado y ficha en `tests/directorio-paginas.test.ts` (marcador decorativo sin `<img>`, sello y etiqueta con sus literales) y verificó que ninguno declara `"use client"`.
- [x] 8. Crear el componente de tarjeta del listado: marcador de foto o foto, nombre, colonia (del catálogo o el texto libre), etiqueta "A domicilio" solo cuando aplica, botón verde de WhatsApp con área táctil ≥44px y etiqueta accesible que nombre al negocio, y el resto de la tarjeta enlazando a la ficha. Verificable: tests de renderizado con un negocio a domicilio y otro sin, y con un negocio de colonia "Otra"; el botón de WhatsApp apunta a `wa.me` y el enlace de la tarjeta a la ficha. — Conectada a datos reales y con tests (`tests/directorio-paginas.test.ts`): contenido, "A domicilio" solo cuando aplica, colonia "Otra", `aria-label` con el nombre del negocio y enlace a la ficha. `coloniaNombre` y `hrefWhatsapp` pasaron a aceptar `null` (negocio sin colonia guardada o con WhatsApp no normalizable).
- [x] 9. Construir la ruta `/[categoria]` (Server Component, lectura dinámica): `h1` "<Categoría> en Tizayuca", listado ordenado por publicación más reciente y luego por nombre, tarjetas de la tarea 8, y `notFound()` cuando el slug no está en el catálogo. Verificable: `/servicios-del-hogar` lista los negocios sembrados de esa categoría; `/plomeros-baratos` responde 404; `/registro` sigue abriendo el formulario. — Conectada a `src/lib/directorio.ts`; el mock quedó eliminado. Orden, 404 de categoría inexistente y `/registro` intacto verificados en tests y con `next start` real.
- [x] 10. Agregar al listado el filtro por colonia como enlaces `?colonia=<slug>` (sin JS de cliente): opción "Todas las colonias" más las colonias con negocios publicados en esa categoría, marca visual de la opción activa, y filtro desconocido ignorado. Verificable: con filtro aplicado solo aparecen los negocios de esa colonia; un `?colonia=inventada` muestra el listado completo sin error. — Sobre datos reales: colonia del catálogo sin resultados y colonia inventada son casos distintos (`obtenerColoniaPorSlug`), ambos con test.
- [x] 11. Agregar los dos estados vacíos del listado con sus textos literales: sin negocios en la categoría ("Todavía no hay negocios publicados en esta categoría." + "Registra tu negocio gratis") y sin resultados con filtro ("No encontramos negocios de esta categoría en esa colonia." + "Ver todas las colonias"). Verificable: tests de ambos casos sobre una categoría vacía y sobre un filtro sin resultados. — Tests de ambos estados vacíos en `tests/directorio-paginas.test.ts`.
- [x] 12. Construir la ruta de ficha `/negocio/[…]` (Server Component): `h1` con el nombre, sello "Negocio verificado", qué ofrece, colonia, dirección o referencias, horario y etiqueta "A domicilio", omitiendo por completo lo que el negocio no registró, y `notFound()` si el identificador no existe o el negocio no está publicado. Verificable: ficha completa vs. ficha con solo obligatorios (sin secciones vacías); la ficha de un negocio `en_revision` responde 404 y su HTML no contiene ningún dato del negocio. — Conectada a la base: el test compara el `digest` del 404 de un id inexistente, uno `en_revision` y uno `rechazado` (idénticos) y `curl` confirma 404 sin ningún dato del negocio.
- [x] 13. Agregar a la ficha los botones de contacto con el módulo de la tarea 4: "Enviar WhatsApp" siempre y como única acción principal en verde, "Llamar" solo con teléfono fijo, "Cómo llegar" solo con dirección o referencias, y el enlace a la página registrada mostrando su dominio real sin nombrar a Facebook (hallazgo M4 de T-003). Todos los que abren pestaña nueva con `rel="noopener noreferrer"`; `tel:` sin pestaña nueva. Verificable: tests de las cuatro combinaciones de campos presentes/ausentes y del enlace con dominio que no es Facebook. — Usa `src/lib/enlaces.ts`; tests de las combinaciones (con/sin teléfono, con/sin dirección, con/sin página) y del dominio que no es Facebook, más la revisión de `rel`/`target` de `tests/layout.test.ts`.
- [x] 14. Reescribir la home (`src/app/page.tsx`): `h1` "¿Qué necesitas en Tizayuca?", la frase de bienvenida, sección "Busca por categoría" con las 8 categorías del catálogo como botones grandes (≥44px) enlazados a su listado, y la entrada al registro bajo "¿Tienes un negocio en Tizayuca?" con "Registra tu negocio gratis". Sin buscador ni controles muertos. Verificable: el HTML de la home trae los 8 nombres del catálogo con enlaces a sus slugs y ya no trae la frase "Muy pronto vas a poder encontrar aquí…".
- [x] 15. Agregar a la home el bloque "Deporte en Tizayuca" con su frase y la entrada "Ver clubes y escuelas deportivas", al mismo nivel visual que el bloque de categorías (mismo nivel de encabezado y mismo peso). Verificable: el HTML trae el título literal y el enlace a `/clubes-y-escuelas-deportivas`; ambos bloques usan `h2`.
- [x] 16. Actualizar `tests/layout.test.ts`: la lista blanca de hrefs debe aceptar las rutas dinámicas nuevas (categoría y ficha) sin dejar de fallar ante un enlace a una ruta inexistente, y los enlaces externos (`https://wa.me`, mapas, página del negocio) deben verificarse aparte, exigiendo `rel="noopener noreferrer"` cuando abren pestaña nueva, con `tel:` exento. Verificable: la suite pasa con los enlaces nuevos y falla si se agrega un href interno inventado o un externo sin `rel`. — `problemasDeEnlaces()` revisa el HTML servido (home, listado con y sin filtro, ficha, 404, footer) y se prueba a sí misma con un href inventado, un externo sin `rel` y un `tel:` con pestaña nueva.
- [x] 17. Actualizar en `tests/layout.test.ts` los scenarios de la home que cambiaron (encabezados nuevos, un solo `h1` con `h2` de sección permitidos, ausencia de la frase provisional, CTA de registro intacto). Verificable: `npm test` en verde con los textos literales de la spec. — home nueva (un `h1`, tres `h2`, sin la frase provisional, CTA con su pregunta) y scenarios de la 404.
- [x] 18. Escribir el test de privacidad del directorio: el HTML de un listado y de una ficha no contiene estado, origen, `registradoEn`, `consintioAvisoEn` ni `tokenGestion`, y la ficha de un negocio sin dirección capturada no muestra ninguna referencia de ubicación además de la colonia. Verificable: `npm test` en verde con negocios sembrados que sí tienen esos campos poblados. — en `tests/directorio-paginas.test.ts` (HTML) y en `tests/directorio-consultas.test.ts` (la proyección ni siquiera lee esos campos).
- [x] 19. Revisar la home, un listado (con y sin filtro), una ficha completa, una ficha mínima y la 404 a 390px, 768px y 1280px: sin scroll horizontal, áreas táctiles ≥44px y nada que compita visualmente con el botón de WhatsApp. Verificable: inspección en los tres anchos y revisión del HTML servido; lo que no se pueda comprobar automáticamente se deja anotado para el humano del PR. — revisión estructural + `next start` real (ver `reports/b-dev.md`); lo que necesita ojos humanos en un navegador queda anotado ahí. Se agregó `break-words`/`break-all` a todo texto que escribe el negocio (un nombre de 80 caracteres sin espacios sacaba la tarjeta de la pantalla a 390px).
- [x] 20. Repasar todos los textos nuevos contra la spec (literales carácter por carácter) y contra el español mexicano coloquial del proyecto, y confirmar que ningún dato ficticio del seed se parece a un negocio real de Tizayuca. Verificable: diff de textos contra los literales de la spec y revisión del archivo de seed de demo. — literales verificados contra el HTML servido; se quitó el comentario de React que partía el `h1` del listado (`Servicios del hogar<!-- --> en Tizayuca`) y el del botón de la página registrada.

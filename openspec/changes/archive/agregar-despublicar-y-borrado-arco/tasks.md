# Tareas: agregar-despublicar-y-borrado-arco

## Modelo de datos

- [x] 1. Agregar a `prisma/schema.prisma` los campos `despublicadoEn` (fecha opcional) y `motivoDespublicacion` (texto opcional) en `Negocio`, y generar la migración. Verificable: una base desde cero migra y corre `npm run db:seed` sin error; `npx prisma migrate status` no reporta drift; el SQL de la migración **no redefine ninguna columna existente ni borra los CHECK de `estado` y `origen`**; una base con negocios publicados, en revisión y rechazados conserva todas sus filas con los dos campos nuevos en nulo. **[DEV, completo]** Migración `20260904141721_agregar_rastro_de_despublicacion` (dos `ALTER TABLE ... ADD COLUMN`, sin redefinir nada). Tests: `tests/modelo-despublicacion.test.ts`.
- [x] 2. Revisar que toda relación que apunta a `Negocio` tenga borrado en cascada (hoy: los vínculos con giros; y los reportes, si `agregar-boton-reportar` ya mergeó) y dejar el invariante cubierto por una prueba. Verificable: test que crea un negocio con giros (y con reportes si existen), lo borra con `deleteMany` y comprueba que no queda ninguna fila ligada ni error de restricción; el test falla si alguien agrega después una relación sin cascada. **[DEV, completo]** El invariante se prueba recorriendo `PRAGMA foreign_key_list` de TODAS las tablas: cualquier relación futura hacia `Negocio` sin cascada rompe la suite. Hoy solo existe `_GiroToNegocio.B`, que ya era `ON DELETE CASCADE`.

## Dominio del panel

- [x] 3. Agregar a `src/lib/admin/textos.ts` todos los literales nuevos: "Despublicar", "¿Por qué la despublicas?", "Escribe por qué la despublicas", "Ya la despublicaste.", "Esta ficha ya no estaba publicada.", la plantilla del WhatsApp de despublicación, "Ya estaba publicada, la despublicaste", "Cuándo la despublicaste", "Por qué la despublicaste", "Borrar definitivamente", "¿Seguro que quieres borrar esta ficha?", la advertencia con el nombre, el recordatorio del trámite ARCO, "Escribe BORRAR para confirmar", "Sí, borrar para siempre", "Mejor no, regresar", "Para borrar, escribe BORRAR en el campo.", "Ya se borró para siempre." y "Esta ficha ya no existe." Verificable: `tests/admin-textos.test.ts` compara cada constante carácter por carácter contra el delta de `revision-admin`. **[UI, completo]** **[DEV]** La comparación carácter por carácter ya existe: `tests/admin-textos.test.ts` (ese archivo sí existía) compara los 19 literales nuevos y las dos plantillas contra el delta de `revision-admin`.
- [x] 4. Agregar `despublicarFicha(prisma, id, motivo, ahora)` a `src/lib/admin/transiciones.ts` con escritura condicionada (`updateMany` con `estado: publicado` en el `where`), motivo obligatorio recortado con la misma cota que el del rechazo, y resultado discriminado (`despublicada | ya-no-publicada | no-encontrado | error: motivo`). Verificable: tests contra la base de prueba de despublicar una ficha publicada (queda `en_revision`, con fecha y motivo), motivo vacío o de puros espacios (no escribe nada), doble llamada (la segunda devuelve `ya-no-publicada` y no pisa la fecha), llamada sobre `en_revision` y sobre `rechazado` (sin efecto), id inexistente, y que giros, colonia, origen, `publicadoEn`, `rechazadoEn` y los datos capturados quedan intactos. **[DEV, completo]** `despublicarFicha` + `LIMITE_MOTIVO_DESPUBLICACION`. Tests en `tests/admin-transiciones.test.ts`.
- [x] 5. Agregar `borrarNegocio(prisma, id)` a `src/lib/admin/transiciones.ts` con `deleteMany` por id (idempotente, sin `delete` que lance) y resultado `borrado | ya-no-existe`. Verificable: tests de borrar un negocio publicado con giros (y reportes si existen), borrar uno `en_revision` y uno `rechazado`, borrar dos veces seguidas (la segunda devuelve `ya-no-existe` sin excepción) y borrar un id inexistente; después de cada borrado, ninguna consulta devuelve el negocio ni sus filas ligadas. **[DEV, completo]** `borrarNegocio` con `deleteMany`. El punto de integración de la foto (T-008) queda documentado en el cuerpo de la función y exigido por un test.
- [x] 6. Ajustar `src/lib/admin/consultas.ts`: la espera de la cola se calcula desde la más reciente entre `registradoEn` y `despublicadoEn`, cada fila expone si llegó por una despublicación, y el detalle devuelve `despublicadoEn` y `motivoDespublicacion`. Verificable: tests unitarios del reloj (registro viejo + despublicación de hace 2 horas → "Hace 2 horas" y no atrasado; despublicación vieja + reenvío reciente → cuenta desde el reenvío; sin despublicación → se comporta exactamente como hoy) y test del detalle con y sin rastro de despublicación. **[DEV, completo]** `entradaALaCola` (`max(registradoEn, despublicadoEn)`) manda el texto de espera, el atraso y el ORDEN de la cola; los tres campos del contrato de la etapa UI dejaron de ser opcionales.

## Pantallas del panel

- [x] 7. Crear el formulario de despublicar (`src/components/admin/formulario-despublicar.tsx`) y su Server Action en `src/app/admin/registros/[id]/accion-despublicar.ts`, con `requerirSesionAdmin()` antes de leer o escribir nada y el patrón POST → `redirect` → GET para los errores (sin `"use client"`). Verificable: test de despublicar con motivo (redirige a la pantalla de confirmación y la ficha queda en `en_revision`), sin motivo (vuelve al detalle con "Escribe por qué la despublicas" y sin tocar la base) y sobre una ficha no publicada ("Esta ficha ya no estaba publicada."). **[UI]** El componente es real. La Server Action es MOCK (`accion-despublicar-mock.ts`, guarda de sesión real, valida motivo vacío, NO escribe en la base): el dev debe crearla en `accion-despublicar.ts` llamando a `despublicarFicha` (tarea 4) y ramificar `ya-no-publicada`/`no-encontrado` (hoy sin destino propio). Sin tests nuevos.
- [x] 8. Crear la pantalla `src/app/admin/registros/[id]/despublicado/page.tsx`, hermana de `aprobado/` y `rechazado/`: "Ya la despublicaste.", botón "Avisarle por WhatsApp" con el mensaje prellenado que incluye el motivo guardado, y "Volver a la cola". Verificable: test que comprueba el mensaje carácter por carácter con un negocio de nombre y motivo conocidos, que el número no normalizable se muestra como texto sin botón, y que recargar la pantalla no repite ninguna acción. **[UI]** Página real, con guarda de estado (`en_revision`) igual que `aprobado`/`rechazado`. El motivo cae a `?motivoMock=` porque `motivoDespublicacion` no existe en el schema todavía (tarea 1) — el dev debe borrar ese fallback en cuanto la columna y la consulta (tarea 6) existan. Sin tests nuevos.
- [x] 9. Crear la pantalla de confirmación del borrado `src/app/admin/registros/[id]/borrar/page.tsx` (Server Component, con guarda de sesión): encabezado, advertencia con el nombre del negocio, recordatorio del trámite ARCO, campo "Escribe BORRAR para confirmar", botón "Sí, borrar para siempre" y "Mejor no, regresar" al detalle. Verificable: la página responde con todos los literales para un registro existente; abrirla, recargarla y navegar hacia atrás no borra nada; sin sesión redirige al acceso sin revelar si el id existe; no declara el modo de cliente. **[UI, completo]** Real y funcional tal cual (GET puro, no borra nada). Sin tests nuevos.
- [x] 10. Crear la Server Action del borrado y la pantalla de "borrado hecho": valida la palabra `BORRAR` (ignorando mayúsculas y espacios sobrantes, ninguna otra palabra), llama a `borrarNegocio` y lleva a una pantalla propia con "Ya se borró para siempre." y la salida a la cola. Verificable: tests de palabra correcta (la fila desaparece), de "borra"/vacío/otra palabra (no borra y muestra "Para borrar, escribe BORRAR en el campo."), de " borrar " (borra), de doble envío ("Esta ficha ya no existe." sin error 500), y de que **ni la URL final ni la pantalla traen nombre, WhatsApp ni id con datos personales**. **[UI]** La pantalla `/admin/borrado-hecho` es real y ya distingue "borrado" de "ya-no-existe" por `?resultado=` (sin datos personales). La Server Action es MOCK (`accion-borrar-mock.ts`, valida la palabra, NO borra nada): el dev debe crearla en `accion-borrar.ts` llamando a `borrarNegocio` (tarea 5) y pasar el `resultado` real. Sin tests nuevos.
- [x] 11. Reorganizar el detalle (`src/app/admin/registros/[id]/page.tsx` + componente de acciones en `src/components/admin/`): datos → contexto (reportes sin atender, cuando exista esa capacidad) → acciones según el estado (aprobar/rechazar solo en `en_revision`, despublicar solo en `publicado`, "Borrar definitivamente" siempre, visualmente separado como acción irreversible). Verificable: tests del detalle en los tres estados comprobando qué formularios aparecen y cuáles no, y que el control de borrar va después de los datos. **[UI, completo]** Orden real: datos → (comentario ancla para los reportes de `agregar-boton-reportar`) → WhatsApp de verificación → aprobar/rechazar (`en_revision`) → despublicar (`publicado`) → "Borrar definitivamente" (siempre, `border-2 border-tinta` para distinguirla sin inventar un color nuevo). Sin tests nuevos.
- [x] 12. Mostrar en la cola la etiqueta "Ya estaba publicada, la despublicaste" en los renglones que llegaron por una despublicación, con el reloj de espera de la tarea 6. Verificable: test de la cola con un registro nuevo y una ficha recién despublicada (registrada hace meses): la despublicada trae la etiqueta, no trae "Lleva más de 48 horas" y el conteo de atrasados no la incluye; los renglones normales no cambian. **[UI, contrato]** `TarjetaCola` ya pinta la etiqueta si `vieneDeDespublicacion` es `true`. El campo es opcional en `RegistroColaItem` porque `obtenerColaDeRevision` (tarea 6) todavía no lo calcula ni implementa el reloj `max(registradoEn, despublicadoEn)` — hasta entonces ningún renglón la muestra (se comporta como hoy). Sin tests nuevos.
- [x] 13. Hacer que el formulario de aprobar llegue con los giros que la ficha ya tiene marcados y comprobar que republicar actualiza `publicadoEn`. Verificable: test que despublica una ficha con 3 giros, abre el detalle (los 3 checkboxes vienen marcados), aprueba sin tocar nada y comprueba que conserva los 3 giros y estrena fecha de publicación; y test de que un registro nuevo sigue llegando sin ningún giro marcado. **[UI, contrato]** `page.tsx` ya premarca con `registro.girosIds ?? []` cuando no hay error que conservar. `girosIds` es opcional en `RegistroAdminDetalle`: `obtenerRegistroParaPanel` (tarea 6) todavía no selecciona la relación `giros`, así que hoy sigue llegando `undefined` → `[]` (igual que un registro nuevo). La parte de que "republicar actualiza `publicadoEn`" ya la hace `aprobarRegistro` (transiciones.ts existente, sin cambios). Sin tests nuevos.

## Páginas legales

- [x] 14. Retirar de `PENDIENTES_OPERATIVOS_LEGALES` (`src/lib/legales/textos.ts`) el renglón de "E3-6 (flujo ARCO en el panel)", dejando el de la purga de rechazados (E0-3). Verificable: la suite de legales comprueba que la lista ya no menciona el flujo ARCO, que sigue trayendo la purga con su ticket, que la lista sigue sin publicarse en ninguna de las dos páginas y que el texto publicado del aviso y de los términos no cambió ni un carácter. **[DEV, completo]** Renglón de E3-6 retirado; la suite de legales ahora exige que NO esté y que la purga siga.

## Superficie pública

- [x] 15. Escribir la suite del directorio público ante una ficha despublicada y una borrada: home, listado con y sin filtro de colonia, conteos, resultados del buscador y ficha. Verificable: tras despublicar, ningún dato del negocio aparece en el HTML de esas pantallas y su URL responde 404 con el mismo cuerpo y código que un id inexistente; tras borrar, lo mismo; y en ninguna aparece la fecha ni el motivo de la despublicación. **[DEV, completo]** `tests/directorio-despublicado.test.ts`.

## Seguridad y cierre

- [x] 16. Extender `tests/admin-adversarial.test.ts` con las acciones nuevas: POST de despublicar sin cookie de sesión, POST de borrado sin cookie (con la palabra correcta incluida), GET de la pantalla de confirmación sin sesión, POST de borrado con un id inexistente y POST de despublicar con el motivo manipulado (arreglo, campo repetido, 10 000 caracteres). Verificable: ninguna produce error del servidor, ninguna escribe ni borra nada, y ninguna respuesta confirma si el identificador existe. **[DEV, completo]** Extendido `tests/admin-adversarial.test.ts`, incluida la lista `ACCESOS_A_DATOS` con las dos funciones nuevas.
- [x] 17. Escribir la suite de no fuga del borrado: el log del servidor durante una despublicación y un borrado no contiene nombre, WhatsApp, teléfono, dirección ni el motivo; y la URL de la pantalla posterior al borrado no lleva ningún dato del negocio. Verificable: la suite falla si alguien mete el nombre en el `redirect` o un `console.log` con datos del registro. **[DEV, completo]** En `tests/admin-despublicar-borrado.test.ts` (log del servidor y URL final).
- [x] 18. Revisar el panel a 390px, 768px y 1280px con el JavaScript de cliente deshabilitado: detalle de una ficha publicada (con reportes si existen), formulario de despublicar, pantalla de confirmación del borrado con un nombre de negocio largo, pantalla posterior al borrado y cola con la etiqueta nueva. Verificable: sin scroll horizontal, áreas táctiles ≥44px, contraste AA, las tres acciones funcionan sin JS; lo que necesite ojos humanos queda anotado para el PR. **[DEV, parcial]** Verificado por HTTP contra `next dev -p 3400` (200 y literales exactos en las 4 pantallas nuevas). El envío de los formularios sin JavaScript NO se pudo reproducir con `curl` (la misma petición contra la acción de rechazar, ya en producción desde T-005, tampoco dispara: es el arnés, no el código). Queda para ojos humanos: los tres anchos, el nombre larguísimo en la confirmación y el envío real con JS deshabilitado.
- [x] 19. Repasar todos los literales nuevos carácter por carácter contra el delta de `revision-admin`, confirmar que están en español mexicano coloquial y que las fixtures de las suites nuevas usan datos ficticios (WhatsApp de la serie `771999xxxx`, nombres inventados). Verificable: diff de literales contra el delta y revisión de las fixtures. **[DEV, completo]** Los 19 literales nuevos se comparan carácter por carácter en `tests/admin-textos.test.ts`; las fixtures nuevas usan la serie `771999 7xxx`/`8xxx`/`9xxx` y nombres inventados.
